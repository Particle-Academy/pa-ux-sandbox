{
  "schemaVersion": 1,
  "name": "particle-academy/fancy-flow-nodes",
  "kind": "@particle-academy/git_issue_update",
  "aliases": [
    "git_issue_update"
  ],
  "configVersion": 1,
  "title": "Update Issue",
  "category": "io",
  "description": "Change an issue's title, body, state or labels.",
  "outputShape": [
    { "path": "id", "type": "string", "description": "id returned by this node." },
    { "path": "number", "type": "number", "description": "number returned by this node." },
    { "path": "title", "type": "string", "description": "title returned by this node." },
    { "path": "state", "type": "string", "description": "state returned by this node." },
    { "path": "webUrl", "type": "string", "description": "webUrl returned by this node." },
    { "path": "author", "type": "string", "description": "author returned by this node." },
    { "path": "labels", "type": "array", "description": "labels returned by this node." },
    { "path": "assignees", "type": "array", "description": "assignees returned by this node." }
  ],
  "ui": [
    "ui"
  ],
  "runtimes": {
    "ts": {
      "files": [
        "js"
      ],
      "engine": ">=0.30.0"
    },
    "php": {
      "files": [
        "php"
      ],
      "engine": ">=0.9.0"
    }
  },
  "capabilities": {
    "gitProvider": "required"
  },
  "fancyDependencies": [
    {
      "package": "fancy-git",
      "npm": "@particle-academy/fancy-git",
      "composer": "particle-academy/fancy-git",
      "reason": "the provider contract this node acts through — needs >=0.2 for IssueProvider",
      "requirement": "required"
    },
    {
      "package": "fancy-git-github",
      "npm": "@particle-academy/fancy-git-github",
      "composer": "particle-academy/fancy-git-github",
      "reason": "the only adapter implementing IssueProvider today — GitLab and Bitbucket do not yet",
      "requirement": "required"
    }
  ],
  "fixtures": "nodes/git-issue-update/fixtures/git-issue-update.json",
  "sideEffects": "idempotent"
}
