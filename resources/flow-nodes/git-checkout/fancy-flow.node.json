{
  "schemaVersion": 1,
  "name": "particle-academy/fancy-flow-nodes",
  "kind": "@particle-academy/git_checkout",
  "aliases": ["git_checkout"],
  "configVersion": 1,
  "title": "Checkout",
  "category": "io",
  "description": "Switch a working copy to a branch or revision — or propose it for approval.",
  "ui": ["ui"],
  "runtimes": {
    "ts": { "files": ["js"], "engine": ">=0.30.0" },
    "php": { "files": ["php"], "engine": ">=0.9.0" }
  },
  "capabilities": { "gitRepository": "required" },
  "fancyDependencies": [
    {
      "package": "fancy-git",
      "npm": "@particle-academy/fancy-git",
      "composer": "particle-academy/fancy-git",
      "reason": "provides the GitRepository the host resolves for this node",
      "requirement": "required"
    }
  ],
  "fixtures": "nodes/git-checkout/fixtures/git-checkout.json",
  "sideEffects": "unsafe-to-replay"
}
