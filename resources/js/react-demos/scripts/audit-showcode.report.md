# ShowCode Audit Report

Generated by `resources/js/react-demos/scripts/audit-showcode.mjs`.

**Total DemoSection blocks:** 291

| Status | Count |
|---|---|
| drift | 148 |
| dynamic-skipped | 56 |
| missing-code | 35 |
| match | 52 |

## Drifts (code prop does not match rendered JSX)

### resources/js/react-demos/pages/AvatarDemo.tsx — With Image

```diff
--- code prop
+++ rendered JSX
- <Avatar size="lg" src="https://i.pravatar.cc/150?u=alice" alt="Alice" />
+ <Avatar
- <Avatar size="lg" src="https://i.pravatar.cc/150?u=bob" alt="Bob" />
+   size="lg"
- <Avatar size="lg" src="https://i.pravatar.cc/150?u=carol" alt="Carol" />
+   src="https://i.pravatar.cc/150?u=alice"
+   alt="Alice"
+ />
+ <Avatar
+   size="lg"
+   src="https://i.pravatar.cc/150?u=bob"
+   alt="Bob"
+ />
+ <Avatar
+   size="lg"
+   src="https://i.pravatar.cc/150?u=carol"
+   alt="Carol"
+ />
```

### resources/js/react-demos/pages/BreadcrumbsDemo.tsx — Custom Separator

```diff
--- code prop
+++ rendered JSX
- <Breadcrumbs separator={<span>/</span>}>
+ <Breadcrumbs separator={<span className="text-zinc-400">/</span>}>
    <Breadcrumbs.Item href="#">src</Breadcrumbs.Item>
    <Breadcrumbs.Item href="#">components</Breadcrumbs.Item>
    <Breadcrumbs.Item active>Button.tsx</Breadcrumbs.Item>
  </Breadcrumbs>
```

### resources/js/react-demos/pages/CalendarDemo.tsx — Multiple Dates

```diff
--- code prop
+++ rendered JSX
  <Calendar
    mode="multiple"
-   value={dates}
+   value={multiple}
-   onChange={(d) => setDates(d)}
+   onChange={(dates) => setMultiple(dates as Date[])}
  />
+ <p className="text-sm text-zinc-500">
+   {multiple.length} date(s) selected
+ </p>
```

### resources/js/react-demos/pages/CanvasDemo.tsx — Basic Canvas

```diff
--- code prop
+++ rendered JSX
- <Canvas showGrid>
+ <Canvas showGrid className="h-full w-full">
    <Canvas.Node id="1" x={50} y={50}>
      <Card><Card.Body>Authentication</Card.Body></Card>
    </Canvas.Node>
    <Canvas.Node id="2" x={300} y={50}>
      <Card><Card.Body>Dashboard</Card.Body></Card>
    </Canvas.Node>
    <Canvas.Node id="3" x={175} y={200}>
      <Card><Card.Body>API Gateway</Card.Body></Card>
    </Canvas.Node>
    <Canvas.Edge from="1" to="3" curve="bezier" />
    <Canvas.Edge from="2" to="3" curve="bezier" />
    <Canvas.Controls />
  </Canvas>
```

### resources/js/react-demos/pages/CanvasDemo.tsx — Edge Types

```diff
--- code prop
+++ rendered JSX
- <Canvas showGrid>
+ <Canvas showGrid className="h-full w-full">
-   <Canvas.Edge from="a" to="d" curve="bezier" label="bezier" />
+   <Canvas.Node id="a" x={50} y={50}>
-   <Canvas.Edge from="b" to="e" curve="step" label="step" />
+     <Card><Card.Body>Source A</Card.Body></Card>
-   <Canvas.Edge from="c" to="f" curve="straight" label="straight" />
+   </Canvas.Node>
- </Canvas>
+   <Canvas.Node id="b" x={300} y={50}>
+     <Card><Card.Body>Source B</Card.Body></Card>
+   </Canvas.Node>
+   <Canvas.Node id="c" x={550} y={50}>
+     <Card><Card.Body>Source C</Card.Body></Card>
+   </Canvas.Node>
+   <Canvas.Node id="d" x={50} y={200}>
+     <Card><Card.Body>Target A</Card.Body></Card>
+   </Canvas.Node>
+   <Canvas.Node id="e" x={300} y={200}>
+     <Card><Card.Body>Target B</Card.Body></Card>
+   </Canvas.Node>
+   <Canvas.Node id="f" x={550} y={200}>
+     <Card><Card.Body>Target C</Card.Body></Card>
+   </Canvas.Node>
+   <Canvas.Edge from="a" to="d" curve="bezier" label="bezier" />
+   <Canvas.Edge from="b" to="e" curve="step" label="step" />
+   <Canvas.Edge from="c" to="f" curve="straight" label="straight" />
+   <Canvas.Controls />
+ </Canvas>
```

### resources/js/react-demos/pages/CanvasDemo.tsx — Pan & Zoom

```diff
--- code prop
+++ rendered JSX
- <Canvas showGrid>
+ <p className="mb-3 text-sm text-zinc-500">
-   <Canvas.Node id="p1" x={0} y={0}>...</Canvas.Node>
+   Scroll to zoom in/out. Click and drag on the background to pan. The minimap in the corner shows your current viewport.
-   <Canvas.Edge from="p1" to="p2" curve="bezier" />
+ </p>
-   <Canvas.Controls />
+ <div style={{ height: 400 }}>
-   <Canvas.Minimap />
+   <Canvas showGrid className="h-full w-full">
- </Canvas>
+     <Canvas.Node id="p1" x={0} y={0}>
+       <Card><Card.Body>Entry Point</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p2" x={250} y={0}>
+       <Card><Card.Body>Router</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p3" x={500} y={0}>
+       <Card><Card.Body>Middleware</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p4" x={0} y={150}>
+       <Card><Card.Body>Auth Service</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p5" x={250} y={150}>
+       <Card><Card.Body>User Service</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p6" x={500} y={150}>
+       <Card><Card.Body>Cache Layer</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p7" x={0} y={300}>
+       <Card><Card.Body>Database</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p8" x={250} y={300}>
+       <Card><Card.Body>Event Bus</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p9" x={500} y={300}>
+       <Card><Card.Body>Logger</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Node id="p10" x={750} y={150}>
+       <Card><Card.Body>CDN</Card.Body></Card>
+     </Canvas.Node>
+     <Canvas.Edge from="p1" to="p2" curve="bezier" />
+     <Canvas.Edge from="p2" to="p3" curve="bezier" />
+     <Canvas.Edge from="p2" to="p5" curve="bezier" />
+     <Canvas.Edge from="p3" to="p6" curve="bezier" />
+     <Canvas.Edge from="p4" to="p7" curve="bezier" />
+     <Canvas.Edge from="p5" to="p8" curve="bezier" />
+     <Canvas.Edge from="p6" to="p9" curve="bezier" />
+     <Canvas.Edge from="p6" to="p10" curve="bezier" />
+     <Canvas.Edge from="p1" to="p4" curve="bezier" />
+     <Canvas.Controls />
+     <Canvas.Minimap />
+   </Canvas>
+ </div>
```

### resources/js/react-demos/pages/CardDemo.tsx — Variants

```diff
--- code prop
+++ rendered JSX
  <Card variant="outlined">
-   <Card.Header>Title</Card.Header>
+   <Card.Header>Outlined</Card.Header>
-   <Card.Body>Content</Card.Body>
+   <Card.Body>Default card style with a border.</Card.Body>
  </Card>
- <Card variant="elevated">...</Card>
+ <Card variant="elevated">
- <Card variant="flat">...</Card>
+   <Card.Header>Elevated</Card.Header>
+   <Card.Body>Card with shadow elevation.</Card.Body>
+ </Card>
+ <Card variant="flat">
+   <Card.Header>Flat</Card.Header>
+   <Card.Body>Subtle background, no border.</Card.Body>
+ </Card>
```

### resources/js/react-demos/pages/CardDemo.tsx — Full Card

```diff
--- code prop
+++ rendered JSX
- <Card>
+ <Card className="max-w-sm">
-   <Card.Header>Title</Card.Header>
+   <Card.Header>
-   <Card.Body>Content here.</Card.Body>
+     <h3 className="font-semibold">Project Update</h3>
-   <Card.Footer>
+     <p className="text-sm text-zinc-500">Last updated 2 hours ago</p>
-     <Action size="sm">View Details</Action>
+   </Card.Header>
-   </Card.Footer>
+   <Card.Body>
- </Card>
+     <p className="text-sm text-zinc-600 dark:text-zinc-400">
+       The latest deployment has been completed successfully. All tests passed
+       and the new features are live in production.
+     </p>
+   </Card.Body>
+   <Card.Footer>
+     <div className="flex gap-2">
+       <Action size="sm">View Details</Action>
+       <Action size="sm" href="#">Share</Action>
+     </div>
+   </Card.Footer>
+ </Card>
```

### resources/js/react-demos/pages/CardDemo.tsx — Padding

```diff
--- code prop
+++ rendered JSX
- <Card padding="sm">...</Card>
+ <Card padding="sm">
- <Card padding="md">...</Card>
+   <Card.Body>Small padding</Card.Body>
- <Card padding="lg">...</Card>
+ </Card>
+ <Card padding="md">
+   <Card.Body>Medium padding</Card.Body>
+ </Card>
+ <Card padding="lg">
+   <Card.Body>Large padding</Card.Body>
+ </Card>
```

### resources/js/react-demos/pages/CarouselDemo.tsx — Wizard Variant

```diff
--- code prop
+++ rendered JSX
- <Carousel variant="wizard">
+ <Carousel className="max-w-lg" variant="wizard">
    <Carousel.Steps />
    <Carousel.Panels>
-     <Carousel.Slide name="Account">Step 1</Carousel.Slide>
+     <Carousel.Slide name="Account">
-     <Carousel.Slide name="Profile">Step 2</Carousel.Slide>
+       <div className="flex h-48 items-center justify-center rounded-lg bg-violet-100 text-lg font-medium dark:bg-violet-900">
-     <Carousel.Slide name="Confirm">Step 3</Carousel.Slide>
+         Step 1: Account Details
-   </Carousel.Panels>
+       </div>
-   <Carousel.Controls />
+     </Carousel.Slide>
- </Carousel>
+     <Carousel.Slide name="Profile">
+       <div className="flex h-48 items-center justify-center rounded-lg bg-cyan-100 text-lg font-medium dark:bg-cyan-900">
+         Step 2: Profile Setup
+       </div>
+     </Carousel.Slide>
+     <Carousel.Slide name="Confirm">
+       <div className="flex h-48 items-center justify-center rounded-lg bg-emerald-100 text-lg font-medium dark:bg-emerald-900">
+         Step 3: Confirmation
+       </div>
+     </Carousel.Slide>
+   </Carousel.Panels>
+   <div className="mt-4 flex justify-end">
+     <Carousel.Controls />
+   </div>
+ </Carousel>
```

### resources/js/react-demos/pages/ChartDemo.tsx — Line Chart

```diff
--- code prop
+++ rendered JSX
- <Chart.Line
+ <Chart.Line labels={months} series={lineSeries} />
-   labels={["Jan", "Feb", "Mar", "Apr", "May", "Jun"]}
-   series={[{ label: "Revenue", data: [12, 19, 14, 25, 22, 30] }]}
- />
```

### resources/js/react-demos/pages/ChartDemo.tsx — Area Chart

```diff
--- code prop
+++ rendered JSX
- <Chart.Area
+ <Chart.Area labels={months} series={areaSeries} />
-   labels={["Jan", "Feb", "Mar", "Apr", "May", "Jun"]}
-   series={[{ label: "Users", data: [200, 450, 380, 520, 610, 780] }]}
- />
```

### resources/js/react-demos/pages/ChartDemo.tsx — Stacked Bar Chart

```diff
--- code prop
+++ rendered JSX
- <Chart.StackedBar
+ <Chart.StackedBar labels={months} series={stackedSeries} />
-   labels={["Jan", "Feb", "Mar", "Apr", "May", "Jun"]}
-   series={[
-     { label: "Organic", data: [20, 35, 30, 45, 40, 55], color: "#6366f1" },
-     { label: "Paid", data: [15, 20, 25, 18, 30, 28], color: "#22c55e" },
-     { label: "Referral", data: [8, 12, 10, 15, 12, 18], color: "#f59e0b" },
-   ]}
- />
```

### resources/js/react-demos/pages/CodeEditorDemo.tsx — Basic Editor

```diff
--- code prop
+++ rendered JSX
- <CodeEditor value={code} onChange={setCode} language="javascript">
+ <CodeEditor value={basicValue} onChange={setBasicValue} language="JavaScript" maxHeight={350} className={editorClass}>
    <CodeEditor.Toolbar />
    <CodeEditor.Panel />
    <CodeEditor.StatusBar />
  </CodeEditor>
```

### resources/js/react-demos/pages/CodeEditorDemo.tsx — Read-Only Code Display

```diff
--- code prop
+++ rendered JSX
- <CodeEditor value={phpCode} language="php" readOnly>
+ <CodeEditor value={SAMPLE_PHP} language="PHP" readOnly maxHeight={300} className={editorClass}>
    <CodeEditor.Panel />
  </CodeEditor>
```

### resources/js/react-demos/pages/CodeEditorDemo.tsx — Custom Toolbar

```diff
--- code prop
+++ rendered JSX
- function RunButton() {
+ <CodeEditor value={langValue} onChange={setLangValue} language="TypeScript" maxHeight={300} className={editorClass}>
-   const { getValue } = useCodeEditor();
+   <CodeEditor.Toolbar>
-   return <Action onClick={() => run(getValue())}>Run</Action>;
+     <RunButton />
- }
+     <CodeEditor.Toolbar.Separator />
- 
+     <FormatButton />
- <CodeEditor value={code} onChange={setCode} language="typescript">
+   </CodeEditor.Toolbar>
-   <CodeEditor.Toolbar>
+   <CodeEditor.Panel />
-     <RunButton />
+   <CodeEditor.StatusBar />
-     <CodeEditor.Toolbar.Separator />
+ </CodeEditor>
-     <FormatButton />
-   </CodeEditor.Toolbar>
-   <CodeEditor.Panel />
-   <CodeEditor.StatusBar />
- </CodeEditor>
```

### resources/js/react-demos/pages/CodeEditorDemo.tsx — Word Wrap & Configuration

```diff
--- code prop
+++ rendered JSX
- <CodeEditor
+ <CodeEditor value={SAMPLE_HTML} language="HTML" wordWrap tabSize={4} maxHeight={300} className={editorClass}>
-   value={code}
+   <CodeEditor.Toolbar />
-   language="html"
+   <CodeEditor.Panel />
-   wordWrap
+   <CodeEditor.StatusBar />
-   tabSize={4}
+ </CodeEditor>
- >
-   <CodeEditor.Toolbar />
-   <CodeEditor.Panel />
-   <CodeEditor.StatusBar />
- </CodeEditor>
```

### resources/js/react-demos/pages/CodeEditorDemo.tsx — Minimal (No Toolbar, No StatusBar)

```diff
--- code prop
+++ rendered JSX
- <CodeEditor value={code} language="javascript" readOnly lineNumbers={false}>
+ <CodeEditor
-   <CodeEditor.Panel />
+   value={`const greeting = "Hello, world!";\nconsole.log(greeting);`}
- </CodeEditor>
+   language="JavaScript"
+   readOnly
+   lineNumbers={false}
+   className={editorClass}
+ >
+   <CodeEditor.Panel />
+ </CodeEditor>
```

### resources/js/react-demos/pages/ColorPickerDemo.tsx — Controlled

```diff
--- code prop
+++ rendered JSX
  <ColorPicker value={color} onChange={setColor} />
+ <p className="mt-3 text-sm text-zinc-500">
+   Selected: <span className="font-medium font-mono">{color}</span>
+ </p>
```

### resources/js/react-demos/pages/CommandDemo.tsx — Command Palette

```diff
--- code prop
+++ rendered JSX
- <Command open={open} onClose={() => setOpen(false)}>
+ <Action onClick={() => setOpen(true)}>Open Command Palette</Action>
-   <Command.Input placeholder="Type a command..." />
+ <p className="mt-2 text-sm text-zinc-500">
-   <Command.List>
+   Or press <kbd className="rounded border border-zinc-300 px-1.5 py-0.5 text-xs dark:border-zinc-600">Ctrl+K</kbd>
-     <Command.Empty>No results found.</Command.Empty>
+ </p>
-     <Command.Group heading="Navigation">
+ 
-       <Command.Item value="home" onSelect={...}>Home</Command.Item>
+ <Command open={open} onClose={() => setOpen(false)}>
-       <Command.Item value="settings" onSelect={...}>Settings</Command.Item>
+   <Command.Input placeholder="Type a command..." />
-     </Command.Group>
+   <Command.List>
-   </Command.List>
+     <Command.Empty>No results found.</Command.Empty>
- </Command>
+     <Command.Group heading="Navigation">
+       <Command.Item value="home" onSelect={() => setOpen(false)}>
+         Home
+       </Command.Item>
+       <Command.Item value="settings" onSelect={() => setOpen(false)}>
+         Settings
+       </Command.Item>
+       <Command.Item value="profile" onSelect={() => setOpen(false)}>
+         Profile
+       </Command.Item>
+     </Command.Group>
+     <Command.Group heading="Actions">
+       <Command.Item value="new-project" onSelect={() => setOpen(false)}>
+         Create New Project
+       </Command.Item>
+       <Command.Item value="invite" onSelect={() => setOpen(false)}>
+         Invite Team Member
+       </Command.Item>
+       <Command.Item value="export" onSelect={() => setOpen(false)}>
+         Export Data
+       </Command.Item>
+     </Command.Group>
+   </Command.List>
+ </Command>
```

### resources/js/react-demos/pages/ComposerDemo.tsx — With Actions

```diff
--- code prop
+++ rendered JSX
  <Composer
    placeholder="Write something..."
-   actions={<><button>Attach</button><button>Emoji</button></>}
+   actions={
- />
+     <>
+       <button type="button" className="rounded p-1.5 text-zinc-400 hover:bg-zinc-100 dark:hover:bg-zinc-800" title="Attach file">
+         <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+           <path d="M21.44 11.05l-9.19 9.19a6 6 0 01-8.49-8.49l9.19-9.19a4 4 0 015.66 5.66l-9.2 9.19a2 2 0 01-2.83-2.83l8.49-8.48" />
+         </svg>
+       </button>
+       <button type="button" className="rounded p-1.5 text-zinc-400 hover:bg-zinc-100 dark:hover:bg-zinc-800" title="Add emoji">
+         <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+           <circle cx="12" cy="12" r="10" /><path d="M8 14s1.5 2 4 2 4-2 4-2" /><line x1="9" y1="9" x2="9.01" y2="9" /><line x1="15" y1="9" x2="15.01" y2="9" />
+         </svg>
+       </button>
+     </>
+   }
+ />
```

### resources/js/react-demos/pages/ContextMenuDemo.tsx — Basic

```diff
--- code prop
+++ rendered JSX
  <ContextMenu>
    <ContextMenu.Trigger>
-     <div>Right-click here</div>
+     <div className="flex h-48 items-center justify-center rounded-lg border-2 border-dashed border-zinc-300 text-zinc-400 dark:border-zinc-600">
-   </ContextMenu.Trigger>
+       Right-click here
-   <ContextMenu.Content>
+     </div>
-     <ContextMenu.Item onClick={() => {}}>Cut</ContextMenu.Item>
+   </ContextMenu.Trigger>
-     <ContextMenu.Item onClick={() => {}}>Copy</ContextMenu.Item>
+   <ContextMenu.Content>
-     <ContextMenu.Separator />
+     <ContextMenu.Item onClick={() => alert("Cut")}>Cut</ContextMenu.Item>
-     <ContextMenu.Item danger>Delete</ContextMenu.Item>
+     <ContextMenu.Item onClick={() => alert("Copy")}>Copy</ContextMenu.Item>
-   </ContextMenu.Content>
+     <ContextMenu.Item onClick={() => alert("Paste")}>Paste</ContextMenu.Item>
- </ContextMenu>
+     <ContextMenu.Separator />
+     <ContextMenu.Item onClick={() => alert("Select All")}>Select All</ContextMenu.Item>
+     <ContextMenu.Separator />
+     <ContextMenu.Item onClick={() => alert("Delete")} danger>
+       Delete
+     </ContextMenu.Item>
+   </ContextMenu.Content>
+ </ContextMenu>
```

### resources/js/react-demos/pages/DiagramDemo.tsx — Relations

```diff
--- code prop
+++ rendered JSX
- const schema = {
+ <Diagram schema={relationsSchema} />
-   entities: [
-     { name: "User", fields: [{ name: "id", type: "bigint", primary: true }, ...] },
-     { name: "Profile", fields: [{ name: "id", type: "bigint", primary: true }, ...] },
-     { name: "Post", fields: [{ name: "id", type: "bigint", primary: true }, ...] },
-     { name: "Tag", fields: [{ name: "id", type: "bigint", primary: true }, ...] },
-   ],
-   relations: [
-     { from: "User", to: "Profile", type: "one-to-one" },
-     { from: "User", to: "Post", type: "one-to-many" },
-     { from: "Post", to: "Tag", type: "many-to-many" },
-   ],
- };
- 
- <Diagram schema={schema} />
```

### resources/js/react-demos/pages/echarts/Bar3DDemo.tsx — Basic 3D Bar

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
-     xAxis3D: { type: "category", data: ["A", "B", ...] },
+     tooltip: {},
-     yAxis3D: { type: "category", data: ["Mon", "Tue", ...] },
+     xAxis3D: { type: "category", data: ["A", "B", "C", "D", "E"] },
-     zAxis3D: { type: "value" },
+     yAxis3D: { type: "category", data: ["Mon", "Tue", "Wed", "Thu", "Fri"] },
-     grid3D: { boxWidth: 100, boxDepth: 80 },
+     zAxis3D: { type: "value" },
-     series: [{
+     grid3D: { boxWidth: 100, boxDepth: 80, viewControl: { distance: 200 } },
-       type: "bar3D",
+     series: [{
-       data: [[x, y, z], ...],
+       type: "bar3D",
-       shading: "lambert",
+       data: generate3DBarData().map((d) => ({
-     }],
+         value: d,
-   }}
+         itemStyle: { opacity: 0.8 },
-   style={{ height: 500 }}
+       })),
- />
+       shading: "lambert",
+       label: { show: false },
+     }],
+   } as any}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/Bar3DDemo.tsx — Styled 3D Bar

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
-     xAxis3D: { type: "category", data: [...] },
+     tooltip: {},
-     yAxis3D: { type: "category", data: [...] },
+     xAxis3D: { type: "category", data: ["Q1", "Q2", "Q3", "Q4"] },
-     zAxis3D: { type: "value" },
+     yAxis3D: { type: "category", data: ["2022", "2023", "2024"] },
-     grid3D: { light: { main: { intensity: 1.2, shadow: true } } },
+     zAxis3D: { type: "value" },
-     visualMap: { min: 0, max: 30, inRange: { color: [...] } },
+     grid3D: { boxWidth: 120, boxDepth: 60, light: { main: { intensity: 1.2, shadow: true } } },
-     series: [{
+     visualMap: { min: 0, max: 30, inRange: { color: ["#313695", "#74add1", "#fee090", "#f46d43", "#a50026"] } },
-       type: "bar3D",
+     series: [{
-       data: [...],
+       type: "bar3D",
-       shading: "realistic",
+       data: Array.from({ length: 12 }, (_, i) => [i % 4, Math.floor(i / 4), Math.round(Math.random() * 25 + 5)]),
-     }],
+       shading: "realistic",
-   }}
+       bevelSize: 1,
-   style={{ height: 500 }}
+     }],
- />
+   } as any}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/BarDemo.tsx — Basic Bar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: ["Mon", "Tue", ...] },
+     xAxis: { type: "category", data: days },
      yAxis: { type: "value" },
-     series: [{ type: "bar", data: [120, 200, 150, ...] }],
+     tooltip: { trigger: "axis" },
-   }}
+     series: [{ type: "bar", data: [120, 200, 150, 80, 70, 110, 130] }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/BarDemo.tsx — Horizontal Bar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     yAxis: { type: "category", data: ["Brazil", "USA", ...] },
+     yAxis: { type: "category", data: ["Brazil", "Indonesia", "USA", "India", "China"] },
      xAxis: { type: "value" },
-     series: [{ type: "bar", data: [18203, 29034, ...] }],
+     tooltip: { trigger: "axis" },
-   }}
+     series: [{ type: "bar", data: [18203, 23489, 29034, 104970, 131744] }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/BarDemo.tsx — Stacked Bar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: days },
      yAxis: { type: "value" },
-     legend: {},
+     tooltip: { trigger: "axis" },
-     series: [
+     legend: {},
-       { name: "Direct", type: "bar", stack: "total", data: [...] },
+     series: [
-       { name: "Email", type: "bar", stack: "total", data: [...] },
+       { name: "Direct", type: "bar", stack: "total", data: [320, 302, 301, 334, 390, 330, 320] },
-     ],
+       { name: "Email", type: "bar", stack: "total", data: [120, 132, 101, 134, 90, 230, 210] },
-   }}
+       { name: "Ads", type: "bar", stack: "total", data: [220, 182, 191, 234, 290, 330, 310] },
- />
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/BarDemo.tsx — With DataZoom

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: Array.from({ length: 40 }, (_, i) => `Item ${i + 1}`) },
      yAxis: { type: "value" },
-     dataZoom: [{ type: "slider", start: 0, end: 50 }],
+     tooltip: { trigger: "axis" },
-     series: [{ type: "bar", data: [...] }],
+     dataZoom: [{ type: "slider", start: 0, end: 50 }],
-   }}
+     series: [{ type: "bar", data: Array.from({ length: 40 }, () => Math.round(Math.random() * 200 + 50)) }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/BoxplotDemo.tsx — Basic Boxplot

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: ["A", "B", ...] },
+     xAxis: { type: "category", data: ["A", "B", "C", "D", "E"] },
      yAxis: { type: "value" },
-     series: [{
+     tooltip: { trigger: "item" },
-       type: "boxplot",
+     series: [{ type: "boxplot", data: boxData }],
-       // [min, Q1, median, Q3, max]
+   }}
-       data: [[655, 850, 940, 980, 1070], ...],
+ />
-     }],
-   }}
- />
```

### resources/js/react-demos/pages/echarts/BoxplotDemo.tsx — Horizontal Boxplot

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     yAxis: { type: "category", data: [...] },
+     yAxis: { type: "category", data: ["Group 1", "Group 2", "Group 3", "Group 4", "Group 5"] },
      xAxis: { type: "value" },
-     series: [
+     tooltip: { trigger: "item" },
-       { type: "boxplot", data: [...] },
+     series: [
-       { type: "scatter", data: [[1100, 0], ...] },
+       { type: "boxplot", data: boxData },
-     ],
+       {
-   }}
+         type: "scatter",
- />
+         data: [[1100, 0], [600, 1], [1000, 3]],
+         itemStyle: { color: "#ee6666" },
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/CalendarDemo.tsx — Basic Calendar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     visualMap: { min: 0, max: 10000, type: "piecewise",
+     tooltip: { position: "top", formatter: (p: any) => `${p.value[0]}: ${p.value[1]}` },
-       pieces: [
+     visualMap: { min: 0, max: 10000, type: "piecewise", orient: "horizontal", left: "center", top: 0,
-         { lte: 1000, color: "#ebedf0" },
+       pieces: [
-         { gt: 1000, lte: 3000, color: "#9be9a8" },
+         { lte: 1000, color: "#ebedf0" },
-         ...
+         { gt: 1000, lte: 3000, color: "#9be9a8" },
-       ] },
+         { gt: 3000, lte: 6000, color: "#40c463" },
-     calendar: { range: "2024", cellSize: [14, 14] },
+         { gt: 6000, lte: 8000, color: "#30a14e" },
-     series: [{
+         { gt: 8000, color: "#216e39" },
-       type: "heatmap",
+       ],
-       coordinateSystem: "calendar",
+     },
-       data: [["2024-01-01", 500], ...],
+     calendar: { top: 60, range: "2024", cellSize: [14, 14] },
-     }],
+     series: [{
-   }}
+       type: "heatmap",
-   style={{ height: 220 }}
+       coordinateSystem: "calendar",
- />
+       data: generateCalendarData("2024"),
+     }],
+   }}
+   style={{ height: 220 }}
+ />
```

### resources/js/react-demos/pages/echarts/CalendarDemo.tsx — Multi-Year Calendar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     visualMap: { min: 0, max: 10000, show: false,
+     tooltip: {},
-       inRange: { color: ["#ebedf0", "#239a3b", ...] } },
+     visualMap: { min: 0, max: 10000, show: false, inRange: { color: ["#ebedf0", "#c6e48b", "#7bc96f", "#239a3b", "#196127"] } },
      calendar: [
        { top: 40, range: "2023", cellSize: [12, 12] },
        { top: 180, range: "2024", cellSize: [12, 12] },
      ],
      series: [
-       { type: "heatmap", coordinateSystem: "calendar",
+       { type: "heatmap", coordinateSystem: "calendar", calendarIndex: 0, data: generateCalendarData("2023") },
-         calendarIndex: 0, data: [...] },
+       { type: "heatmap", coordinateSystem: "calendar", calendarIndex: 1, data: generateCalendarData("2024") },
-       { type: "heatmap", coordinateSystem: "calendar",
+     ],
-         calendarIndex: 1, data: [...] },
+   }}
-     ],
+   style={{ height: 340 }}
-   }}
+ />
-   style={{ height: 340 }}
- />
```

### resources/js/react-demos/pages/echarts/CandlestickDemo.tsx — Basic Candlestick

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: ["2024-01", ...] },
+     xAxis: { type: "category", data: dates },
-     yAxis: { type: "value" },
+     yAxis: { type: "value", min: 0 },
      tooltip: { trigger: "axis", axisPointer: { type: "cross" } },
-     // [open, close, low, high]
+     series: [{ type: "candlestick", data: stockData }],
-     series: [{ type: "candlestick", data: [[20, 34, 10, 38], ...] }],
+   }}
-   }}
+ />
- />
```

### resources/js/react-demos/pages/echarts/CandlestickDemo.tsx — With Volume

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: dates },
-     yAxis: [{ type: "value" }, { type: "value" }],
+     yAxis: [
-     dataZoom: [{ type: "inside" }],
+       { type: "value", min: 0, position: "left" },
-     series: [
+       { type: "value", position: "right", splitLine: { show: false } },
-       { name: "Price", type: "candlestick", data: [...] },
+     ],
-       { name: "Volume", type: "bar", yAxisIndex: 1, data: [...] },
+     tooltip: { trigger: "axis", axisPointer: { type: "cross" } },
-     ],
+     dataZoom: [{ type: "inside", start: 0, end: 100 }],
-   }}
+     legend: {},
- />
+     series: [
+       { name: "Price", type: "candlestick", data: stockData },
+       {
+         name: "Volume",
+         type: "bar",
+         yAxisIndex: 1,
+         data: [1200, 800, 1500, 2000, 900, 1100, 1400, 1600],
+         itemStyle: { color: "#91cc75", opacity: 0.5 },
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/CustomDemo.tsx — Custom Rectangles

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: ["A", "B", ...] },
+     xAxis: { type: "category", data: ["A", "B", "C", "D", "E"] },
      yAxis: { type: "value" },
-     series: [{
+     tooltip: {},
-       type: "custom",
+     series: [{
-       renderItem: (params, api) => ({
+       type: "custom",
-         type: "rect",
+       renderItem: (_params: any, api: any) => {
-         shape: { x: ..., y: ..., width: ..., height: ... },
+         const xValue = api.value(0);
-         style: api.style(),
+         const yValue = api.value(1);
-       }),
+         const point = api.coord([xValue, yValue]);
-       data: [[0, 50], [1, 80], ...],
+         const size = api.size([1, yValue]);
-       encode: { x: 0, y: 1 },
+         return {
-     }],
+           type: "rect",
-   }}
+           shape: {
- />
+             x: point[0] - size[0] / 4,
+             y: point[1],
+             width: size[0] / 2,
+             height: size[1],
+           },
+           style: {
+             ...api.style(),
+             fill: api.visual("color"),
+           },
+         };
+       },
+       data: [[0, 50], [1, 80], [2, 60], [3, 90], [4, 70]],
+       encode: { x: 0, y: 1 },
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/CustomDemo.tsx — Error Bars

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: ["Mon", "Tue", "Wed", "Thu", "Fri"] },
      yAxis: { type: "value" },
-     series: [
+     tooltip: { trigger: "axis" },
-       { type: "line", data: [120, 200, ...] },
+     series: [
-       {
+       { type: "line", data: [120, 200, 150, 80, 100] },
-         type: "custom",
+       {
-         renderItem: (params, api) => ({
+         type: "custom",
-           type: "group",
+         renderItem: (_params: any, api: any) => {
-           children: [/* error bar lines */],
+           const xValue = api.value(0);
-         }),
+           const high = api.coord([xValue, api.value(1)]);
-         data: [[0, 140, 100], ...],
+           const low = api.coord([xValue, api.value(2)]);
-         encode: { x: 0, y: [1, 2] },
+           const halfWidth = 6;
-       },
+           return {
-     ],
+             type: "group",
-   }}
+             children: [
- />
+               { type: "line", shape: { x1: high[0], y1: high[1], x2: low[0], y2: low[1] }, style: { stroke: "#333", lineWidth: 1.5 } },
+               { type: "line", shape: { x1: high[0] - halfWidth, y1: high[1], x2: high[0] + halfWidth, y2: high[1] }, style: { stroke: "#333", lineWidth: 1.5 } },
+               { type: "line", shape: { x1: low[0] - halfWidth, y1: low[1], x2: low[0] + halfWidth, y2: low[1] }, style: { stroke: "#333", lineWidth: 1.5 } },
+             ],
+           };
+         },
+         data: [[0, 140, 100], [1, 230, 170], [2, 180, 120], [3, 100, 60], [4, 130, 70]],
+         encode: { x: 0, y: [1, 2] },
+         z: 100,
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/EffectScatterDemo.tsx — Basic Effect Scatter

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "value" },
+     tooltip: { trigger: "item" },
-     yAxis: { type: "value" },
+     xAxis: { type: "value" },
-     series: [{
+     yAxis: { type: "value" },
-       type: "effectScatter",
+     series: [
-       symbolSize: 14,
+       {
-       rippleEffect: { period: 3, scale: 3, brushType: "stroke" },
+         type: "effectScatter",
-       data: [[10, 20], [30, 40], [50, 60], [70, 80]],
+         symbolSize: 14,
-     }],
+         rippleEffect: { period: 3, scale: 3, brushType: "stroke" },
-   }}
+         data: [
- />
+           [10, 20],
+           [30, 40],
+           [50, 60],
+           [70, 80],
+           [90, 50],
+         ],
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/EffectScatterDemo.tsx — Highlighted Hotspots

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "value" },
+     tooltip: { trigger: "item" },
-     yAxis: { type: "value" },
+     xAxis: { type: "value" },
-     series: [
+     yAxis: { type: "value" },
-       { type: "scatter", data: regularPoints, symbolSize: 8 },
+     series: [
        {
-         type: "effectScatter",
+         type: "scatter",
-         symbolSize: 20,
+         symbolSize: 8,
-         rippleEffect: { period: 4, scale: 4, brushType: "fill" },
+         itemStyle: { color: "#94a3b8" },
-         data: hotspots,
+         data: Array.from({ length: 40 }, () => [
-       },
+           +(Math.random() * 100).toFixed(1),
-     ],
+           +(Math.random() * 100).toFixed(1),
-   }}
+         ]),
- />
+       },
+       {
+         type: "effectScatter",
+         symbolSize: 20,
+         rippleEffect: { period: 4, scale: 4, brushType: "fill" },
+         itemStyle: { color: "#ef4444" },
+         data: [
+           [25, 80],
+           [55, 45],
+           [85, 20],
+         ],
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/EffectScatterDemo.tsx — With Labels

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "value" },
+     tooltip: { trigger: "item" },
-     yAxis: { type: "value" },
+     xAxis: { type: "value", max: 800 },
-     series: [{
+     yAxis: { type: "value", max: 100, inverse: true },
-       type: "effectScatter",
+     series: [
-       symbolSize: 16,
+       {
-       label: {
+         type: "effectScatter",
-         show: true,
+         symbolSize: 16,
-         position: "right",
+         rippleEffect: { period: 3, scale: 3, brushType: "stroke" },
-         formatter: "{b}",
+         label: {
-       },
+           show: true,
-       data: [
+           position: "right",
-         { name: "San Francisco", value: [122, 38] },
+           formatter: "{b}",
-         { name: "New York", value: [300, 40] },
+           color: "#64748b",
-         { name: "London", value: [480, 52] },
+         },
-         { name: "Tokyo", value: [720, 36] },
+         itemStyle: { color: "#3b82f6" },
-       ],
+         data: cities.map((c) => ({ name: c.name, value: c.coord })),
-     }],
+       },
-   }}
+     ],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/FunnelDemo.tsx — Basic Funnel

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     tooltip: { trigger: "item" },
+     tooltip: { trigger: "item", formatter: "{b}: {c}%" },
-     series: [{
+     legend: {},
-       type: "funnel",
+     series: [{ type: "funnel", data: funnelData, label: { position: "inside" } }],
-       data: [
+   }}
-         { value: 100, name: "Visits" },
+ />
-         { value: 80, name: "Inquiries" },
-         { value: 60, name: "Orders" },
-       ],
-       label: { position: "inside" },
-     }],
-   }}
- />
```

### resources/js/react-demos/pages/echarts/FunnelDemo.tsx — Ascending Funnel

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: { trigger: "item" },
-       type: "funnel",
+     legend: {},
-       sort: "ascending",
+     series: [{
-       data: [...],
+       type: "funnel",
-       gap: 4,
+       sort: "ascending",
-       itemStyle: { borderColor: "#fff", borderWidth: 2 },
+       data: funnelData,
-     }],
+       label: { position: "inside" },
-   }}
+       gap: 4,
- />
+       itemStyle: { borderColor: "#fff", borderWidth: 2 },
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/FunnelDemo.tsx — Comparison Funnel

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [
+     tooltip: { trigger: "item" },
-       { type: "funnel", left: "5%", width: "40%", data: [...] },
+     legend: {},
-       { type: "funnel", left: "55%", width: "40%", data: [...] },
+     series: [
-     ],
+       {
-   }}
+         type: "funnel",
- />
+         left: "5%",
+         width: "40%",
+         data: funnelData,
+         label: { position: "inside" },
+         funnelAlign: "left",
+       },
+       {
+         type: "funnel",
+         left: "55%",
+         width: "40%",
+         data: [
+           { value: 95, name: "Visits" },
+           { value: 60, name: "Inquiries" },
+           { value: 45, name: "Orders" },
+           { value: 30, name: "Payments" },
+           { value: 15, name: "Completed" },
+         ],
+         label: { position: "inside" },
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/GaugeDemo.tsx — Basic Gauge

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: { formatter: "{b}: {c}%" },
-       type: "gauge",
+     series: [{
-       data: [{ value: 72, name: "Performance" }],
+       type: "gauge",
-       detail: { formatter: "{value}%" },
+       data: [{ value: 72, name: "Performance" }],
-     }],
+       detail: { formatter: "{value}%" },
-   }}
+     }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/GaugeDemo.tsx — Multi-Gauge

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      series: [
-       { type: "gauge", center: ["25%", "55%"], radius: "60%",
+       {
-         data: [{ value: 85, name: "CPU" }] },
+         type: "gauge",
-       { type: "gauge", center: ["75%", "55%"], radius: "60%",
+         center: ["25%", "55%"],
-         data: [{ value: 42, name: "Memory" }] },
+         radius: "60%",
-     ],
+         min: 0,
-   }}
+         max: 100,
- />
+         data: [{ value: 85, name: "CPU" }],
+         detail: { formatter: "{value}%", fontSize: 14 },
+         title: { fontSize: 12 },
+       },
+       {
+         type: "gauge",
+         center: ["75%", "55%"],
+         radius: "60%",
+         min: 0,
+         max: 100,
+         data: [{ value: 42, name: "Memory" }],
+         detail: { formatter: "{value}%", fontSize: 14 },
+         title: { fontSize: 12 },
+         itemStyle: { color: "#91cc75" },
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/GaugeDemo.tsx — Gradient Gauge

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      series: [{
        type: "gauge",
        axisLine: {
          lineStyle: {
            width: 20,
            color: [[0.3, "#67e0e3"], [0.7, "#37a2da"], [1, "#fd666d"]],
          },
        },
-       data: [{ value: 65, name: "Score" }],
+       pointer: { itemStyle: { color: "auto" } },
-     }],
+       data: [{ value: 65, name: "Score" }],
-   }}
+       detail: { formatter: "{value}", fontSize: 20 },
- />
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/GlobeDemo.tsx — Basic Globe

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
      globe: {
        baseColor: "#1a3b5c",
        shading: "color",
-       atmosphere: { show: true, color: "#4682b4" },
+       atmosphere: { show: true, color: "#4682b4", glowPower: 6, innerGlowPower: 2 },
-       viewControl: { autoRotate: true },
+       viewControl: { autoRotate: true, autoRotateSpeed: 3, distance: 200 },
      },
-   }}
+   } as any}
    style={{ height: 500 }}
  />
```

### resources/js/react-demos/pages/echarts/GlobeDemo.tsx — Globe with Points

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
      globe: {
        baseColor: "#264653",
        shading: "lambert",
        atmosphere: { show: true },
-       viewControl: { autoRotate: true },
+       viewControl: { autoRotate: true, autoRotateSpeed: 2 },
-     },
+       light: { ambient: { intensity: 0.4 }, main: { intensity: 1.2 } },
-     series: [{
+     },
-       type: "scatter3D",
+     series: [{
-       coordinateSystem: "globe",
+       type: "scatter3D",
-       data: [
+       coordinateSystem: "globe",
-         { name: "New York", value: [-74, 40.7, 100] },
+       symbolSize: 12,
-         { name: "London", value: [-0.1, 51.5, 100] },
+       data: [
-         ...
+         { name: "New York", value: [-74.006, 40.7128, 100] },
-       ],
+         { name: "London", value: [-0.1276, 51.5074, 100] },
-     }],
+         { name: "Tokyo", value: [139.6917, 35.6895, 100] },
-   }}
+         { name: "Sydney", value: [151.2093, -33.8688, 100] },
-   style={{ height: 500 }}
+         { name: "São Paulo", value: [-46.6333, -23.5505, 100] },
- />
+         { name: "Mumbai", value: [72.8777, 19.076, 100] },
+       ],
+       label: { show: true, formatter: "{b}", position: "right", textStyle: { color: "#fff", fontSize: 11 } },
+       itemStyle: { color: "#fac858" },
+     }],
+   } as any}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/GraphDemo.tsx — Force Layout

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: {},
-       type: "graph",
+     legend: { data: categories.map((c) => c.name) },
-       layout: "force",
+     series: [{
-       data: [{ id: "0", name: "Node 0", symbolSize: 40 }, ...],
+       type: "graph",
-       links: [{ source: "0", target: "1" }, ...],
+       layout: "force",
-       roam: true,
+       data: graphNodes,
-       force: { repulsion: 200, edgeLength: 100 },
+       links: graphLinks,
-       emphasis: { focus: "adjacency" },
+       categories,
-     }],
+       roam: true,
-   }}
+       label: { show: true, position: "right" },
-   style={{ height: 500 }}
+       force: { repulsion: 200, edgeLength: 100 },
- />
+       lineStyle: { color: "source", curveness: 0.1 },
+       emphasis: { focus: "adjacency" },
+     }],
+   }}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/GraphDemo.tsx — Circular Layout

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: {},
-       type: "graph",
+     legend: { data: categories.map((c) => c.name) },
-       layout: "circular",
+     series: [{
-       data: [...],
+       type: "graph",
-       links: [...],
+       layout: "circular",
-       roam: true,
+       data: graphNodes,
-       circular: { rotateLabel: true },
+       links: graphLinks,
-     }],
+       categories,
-   }}
+       roam: true,
-   style={{ height: 500 }}
+       label: { show: true },
- />
+       circular: { rotateLabel: true },
+       lineStyle: { color: "source", curveness: 0.3 },
+     }],
+   }}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/GraphicDemo.tsx — Basic Shapes

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      graphic: {
-       elements: [{
+       elements: [
-         type: "group", left: "center", top: "center",
+         {
-         children: [
+           type: "group",
-           { type: "rect", shape: { width: 200, height: 80, r: 10 },
+           left: "center",
-             style: { fill: "#5470c6" } },
+           top: "center",
-           { type: "text", style: { text: "Hello", fill: "#fff",
+           children: [
-             textAlign: "center", textVerticalAlign: "middle" } },
+             {
-         ],
+               type: "rect",
-       }],
+               shape: { x: -100, y: -40, width: 200, height: 80, r: 10 },
-     },
+               style: { fill: "#5470c6", stroke: "#333", lineWidth: 2 },
-   }}
+             },
- />
+             {
+               type: "text",
+               style: {
+                 text: "ECharts Graphic",
+                 x: 0,
+                 y: 0,
+                 fill: "#fff",
+                 fontSize: 18,
+                 fontWeight: "bold",
+                 textAlign: "center",
+                 textVerticalAlign: "middle",
+               },
+             },
+           ],
+         },
+       ],
+     },
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/GraphicDemo.tsx — Circles & Text

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      graphic: {
        elements: [
-         { type: "circle", shape: { cx: 150, cy: 150, r: 60 },
+         {
-           style: { fill: "#91cc75", opacity: 0.8 } },
+           type: "circle",
-         { type: "text", style: { text: "Label", fill: "#666" } },
+           shape: { cx: 150, cy: 150, r: 60 },
-       ],
+           style: { fill: "#91cc75", opacity: 0.8 },
-     },
+         },
-   }}
+         {
- />
+           type: "circle",
+           shape: { cx: 250, cy: 180, r: 45 },
+           style: { fill: "#fac858", opacity: 0.8 },
+         },
+         {
+           type: "circle",
+           shape: { cx: 200, cy: 100, r: 35 },
+           style: { fill: "#ee6666", opacity: 0.8 },
+         },
+         {
+           type: "text",
+           left: "center",
+           bottom: 30,
+           style: {
+             text: "Overlapping circles with transparency",
+             fill: "#666",
+             fontSize: 14,
+           },
+         },
+       ],
+     },
+   }}
+   style={{ height: 350 }}
+ />
```

### resources/js/react-demos/pages/echarts/GraphicDemo.tsx — Stroke Animation

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      graphic: {
-       elements: [{
+       elements: [
-         type: "circle",
+         {
-         shape: { cx: 200, cy: 120, r: 60 },
+           type: "circle",
-         style: { fill: "none", stroke: "#5470c6",
+           shape: { cx: 120, cy: 120, r: 60 },
-           lineWidth: 3, lineDash: [377], lineDashOffset: 377 },
+           style: {
-         keyframeAnimation: {
+             fill: "none",
-           duration: 2000, loop: true,
+             stroke: "#5470c6",
-           keyframes: [
+             lineWidth: 3,
-             { percent: 0.5, style: { lineDashOffset: 0 } },
+             lineDash: [377],
-             { percent: 1, style: { lineDashOffset: -377 } },
+             lineDashOffset: 377,
-           ],
+           },
-         },
+           keyframeAnimation: {
-       }],
+             duration: 2000,
-     },
+             loop: true,
-   }}
+             keyframes: [
- />
+               { percent: 0.5, style: { lineDashOffset: 0 }, easing: "cubicOut" },
+               { percent: 1, style: { lineDashOffset: -377 }, easing: "cubicOut" },
+             ],
+           },
+         },
+         {
+           type: "polygon",
+           shape: {
+             points: [
+               [370, 60],
+               [310, 180],
+               [430, 180],
+             ],
+           },
+           style: {
+             fill: "none",
+             stroke: "#91cc75",
+             lineWidth: 3,
+             lineDash: [360],
+             lineDashOffset: 360,
+           },
+           keyframeAnimation: {
+             duration: 2500,
+             delay: 300,
+             loop: true,
+             keyframes: [
+               { percent: 0.5, style: { lineDashOffset: 0 }, easing: "cubicOut" },
+               { percent: 1, style: { lineDashOffset: -360 }, easing: "cubicOut" },
+             ],
+           },
+         },
+         {
+           type: "rect",
+           shape: { x: 530, y: 60, width: 120, height: 120 },
+           style: {
+             fill: "none",
+             stroke: "#fac858",
+             lineWidth: 3,
+             lineDash: [480],
+             lineDashOffset: 480,
+           },
+           keyframeAnimation: {
+             duration: 2000,
+             delay: 600,
+             loop: true,
+             keyframes: [
+               { percent: 0.5, style: { lineDashOffset: 0 }, easing: "cubicOut" },
+               { percent: 1, style: { lineDashOffset: -480 }, easing: "cubicOut" },
+             ],
+           },
+         },
+         {
+           type: "polyline",
+           shape: {
+             points: [
+               [780, 65], [795, 110], [840, 110], [803, 137], [816, 180],
+               [780, 155], [744, 180], [757, 137], [720, 110], [765, 110], [780, 65],
+             ],
+           },
+           style: {
+             fill: "none",
+             stroke: "#ee6666",
+             lineWidth: 3,
+             lineDash: [400],
+             lineDashOffset: 400,
+           },
+           keyframeAnimation: {
+             duration: 2500,
+             delay: 900,
+             loop: true,
+             keyframes: [
+               { percent: 0.5, style: { lineDashOffset: 0 }, easing: "cubicOut" },
+               { percent: 1, style: { lineDashOffset: -400 }, easing: "cubicOut" },
+             ],
+           },
+         },
+         // Labels
+         {
+           type: "text",
+           left: 90,
+           top: 200,
+           style: { text: "Circle", fill: "#5470c6", fontSize: 12 },
+         },
+         {
+           type: "text",
+           left: 345,
+           top: 200,
+           style: { text: "Triangle", fill: "#91cc75", fontSize: 12 },
+         },
+         {
+           type: "text",
+           left: 560,
+           top: 200,
+           style: { text: "Square", fill: "#fac858", fontSize: 12 },
+         },
+         {
+           type: "text",
+           left: 762,
+           top: 200,
+           style: { text: "Star", fill: "#ee6666", fontSize: 12 },
+         },
+       ],
+     },
+   }}
+   style={{ height: 260 }}
+ />
```

### resources/js/react-demos/pages/echarts/GraphicDemo.tsx — Interactive Graphic

```diff
--- code prop
+++ rendered JSX
- <EChart
+ <InteractiveGraphicDemo />
-   option={{
-     graphic: {
-       elements: [{
-         type: "rect",
-         shape: { width: 120, height: 50 },
-         style: { fill: "#5470c6", text: "Click Me" },
-         onclick: () => { /* handle click */ },
-       }],
-     },
-   }}
-   onEvents={{ click: (params) => console.log(params) }}
- />
```

### resources/js/react-demos/pages/echarts/HeatmapDemo.tsx — Basic Heatmap

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: ["0:00", "1:00", ...] },
+     tooltip: { position: "top" },
-     yAxis: { type: "category", data: ["Mon", "Tue", ...] },
+     xAxis: { type: "category", data: hours, splitArea: { show: true } },
-     visualMap: { min: 0, max: 10, calculable: true },
+     yAxis: { type: "category", data: days, splitArea: { show: true } },
-     series: [{
+     visualMap: { min: 0, max: 10, calculable: true, orient: "horizontal", left: "center", bottom: 0 },
-       type: "heatmap",
+     series: [{
-       data: [[x, y, value], ...],
+       type: "heatmap",
-       label: { show: true },
+       data: generateHeatmapData(),
-     }],
+       label: { show: true },
-   }}
+       emphasis: { itemStyle: { shadowBlur: 10, shadowColor: "rgba(0,0,0,0.5)" } },
-   style={{ height: 350 }}
+     }],
- />
+   }}
+   style={{ height: 350 }}
+ />
```

### resources/js/react-demos/pages/echarts/HeatmapDemo.tsx — Large Heatmap

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     tooltip: { position: "top" },
-     yAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: hours },
-     visualMap: {
+     yAxis: { type: "category", data: days },
-       min: 0, max: 10,
+     visualMap: {
-       inRange: { color: ["#313695", "#fee090", "#d73027"] },
+       min: 0,
-     },
+       max: 10,
-     series: [{ type: "heatmap", data: [...] }],
+       inRange: { color: ["#313695", "#4575b4", "#74add1", "#abd9e9", "#fee090", "#fdae61", "#f46d43", "#d73027"] },
-   }}
+       orient: "horizontal",
- />
+       left: "center",
+       bottom: 0,
+     },
+     series: [{
+       type: "heatmap",
+       data: generateHeatmapData(),
+       emphasis: { itemStyle: { shadowBlur: 10 } },
+     }],
+   }}
+   style={{ height: 350 }}
+ />
```

### resources/js/react-demos/pages/echarts/LineDemo.tsx — Basic Line

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: ["Jan", "Feb", ...] },
+     xAxis: { type: "category", data: months },
      yAxis: { type: "value" },
-     series: [{ type: "line", data: [150, 230, 224, ...] }],
+     tooltip: { trigger: "axis" },
-   }}
+     series: [{ type: "line", data: [150, 230, 224, 218, 135, 147, 260] }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/LineDemo.tsx — Smooth Line

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: months },
      yAxis: { type: "value" },
-     series: [{
+     tooltip: { trigger: "axis" },
-       type: "line",
+     series: [
-       data: [...],
+       {
-       smooth: true,
+         type: "line",
-       areaStyle: { opacity: 0.3 },
+         data: [820, 932, 901, 934, 1290, 1330, 1320],
-     }],
+         smooth: true,
-   }}
+         areaStyle: { opacity: 0.3 },
- />
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/LineDemo.tsx — Multi-Series

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: months },
      yAxis: { type: "value" },
-     legend: {},
+     tooltip: { trigger: "axis" },
-     series: [
+     legend: {},
-       { name: "Email", type: "line", data: [...] },
+     series: [
-       { name: "Search", type: "line", data: [...] },
+       { name: "Email", type: "line", data: [120, 132, 101, 134, 90, 230, 210] },
-     ],
+       { name: "Search", type: "line", data: [220, 182, 191, 234, 290, 330, 310] },
-   }}
+       { name: "Direct", type: "line", data: [150, 232, 201, 154, 190, 330, 410] },
- />
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/LineDemo.tsx — Stacked Area

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...], boundaryGap: false },
+     xAxis: { type: "category", data: months, boundaryGap: false },
      yAxis: { type: "value" },
-     legend: {},
+     tooltip: { trigger: "axis" },
-     series: [
+     legend: {},
-       { name: "A", type: "line", stack: "total", areaStyle: {}, data: [...] },
+     series: [
-       { name: "B", type: "line", stack: "total", areaStyle: {}, data: [...] },
+       { name: "A", type: "line", stack: "total", areaStyle: {}, data: [120, 132, 101, 134, 90, 230, 210] },
-     ],
+       { name: "B", type: "line", stack: "total", areaStyle: {}, data: [220, 182, 191, 234, 290, 330, 310] },
-   }}
+       { name: "C", type: "line", stack: "total", areaStyle: {}, data: [150, 232, 201, 154, 190, 330, 410] },
- />
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/MapDemo.tsx — Scatter on Geo

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { show: false, min: 0, max: 100 },
+     title: { text: "City Populations (simulated positions)", left: "center" },
-     yAxis: { show: false, min: 0, max: 100 },
+     tooltip: {
-     series: [{
+       trigger: "item",
-       type: "effectScatter",
+       formatter: (params: any) => `${params.name}: ${params.value[2]}M`,
-       data: [
+     },
-         { name: "New York", value: [25, 60, 8.3] },
+     xAxis: { show: false, min: 0, max: 100 },
-         { name: "London", value: [45, 75, 9.0] },
+     yAxis: { show: false, min: 0, max: 100 },
-         ...
+     series: [{
-       ],
+       type: "effectScatter",
-       symbolSize: (val) => val[2] * 3,
+       coordinateSystem: "cartesian2d",
-       rippleEffect: { brushType: "stroke" },
+       symbolSize: (val: number[]) => val[2] * 3,
-     }],
+       data: [
-   }}
+         { name: "New York", value: [25, 60, 8.3] },
- />
+         { name: "London", value: [45, 75, 9.0] },
+         { name: "Tokyo", value: [80, 65, 13.9] },
+         { name: "Sydney", value: [75, 20, 5.3] },
+         { name: "São Paulo", value: [30, 30, 12.3] },
+         { name: "Cairo", value: [55, 50, 9.5] },
+       ],
+       label: { show: true, formatter: "{b}", position: "right" },
+       rippleEffect: { brushType: "stroke" },
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/MapDemo.tsx — Note

```diff
--- code prop
+++ rendered JSX
- // Register map data first:
+ <p style={{ padding: 16, color: "#6b7280" }}>
- echarts.registerMap("mapName", geoJSON);
+   Full map support requires GeoJSON data registration. Use{" "}
- 
+   <code>echarts.registerMap(&apos;mapName&apos;, geoJSON)</code> before rendering.
- <EChart
+   The EChart component supports map series once the map data is registered.
-   option={{
+ </p>
-     series: [{
-       type: "map",
-       map: "mapName",
-       data: [...],
-     }],
-   }}
- />
```

### resources/js/react-demos/pages/echarts/ParallelDemo.tsx — Basic Parallel

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      parallelAxis: [
        { dim: 0, name: "Index" },
        { dim: 1, name: "Score A" },
        { dim: 2, name: "Score B" },
-       ...
+       { dim: 3, name: "Score C" },
-     ],
+       { dim: 4, name: "Ratio" },
-     series: [{
+     ],
-       type: "parallel",
+     parallel: { left: "5%", right: "13%", bottom: "10%", top: "20%" },
-       lineStyle: { width: 2, opacity: 0.6 },
+     series: [{
-       data: [[1, 34, 5, 80, 1.5], ...],
+       type: "parallel",
-     }],
+       lineStyle: { width: 2, opacity: 0.6 },
-   }}
+       data: parallelData,
- />
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/ParallelDemo.tsx — Interactive Parallel

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      parallelAxis: [
        { dim: 0, name: "Index", type: "value" },
-       ...
+       { dim: 1, name: "Score A", type: "value" },
-     ],
+       { dim: 2, name: "Score B", type: "value" },
-     parallel: {
+       { dim: 3, name: "Score C", type: "value" },
-       parallelAxisDefault: {
+       { dim: 4, name: "Ratio", type: "value" },
-         areaSelectStyle: { width: 20 },
+     ],
-       },
+     parallel: {
-     },
+       left: "5%",
-     series: [{
+       right: "13%",
-       type: "parallel",
+       parallelAxisDefault: {
-       data: [...],
+         type: "value",
-       smooth: true,
+         nameLocation: "end",
-     }],
+         nameTextStyle: { fontSize: 12 },
-   }}
+         areaSelectStyle: { width: 20 },
- />
+       },
+     },
+     series: [{
+       type: "parallel",
+       lineStyle: { width: 1.5, opacity: 0.5 },
+       data: parallelData,
+       smooth: true,
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/PictorialBarDemo.tsx — Basic Pictorial Bar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: ["Jan", "Feb", ...] },
+     xAxis: { type: "category", data: categories },
      yAxis: { type: "value" },
-     series: [{
+     tooltip: { trigger: "axis" },
-       type: "pictorialBar",
+     series: [{
-       data: [120, 200, ...],
+       type: "pictorialBar",
-       symbol: "diamond",
+       data: values,
-       symbolRepeat: true,
+       symbol: "diamond",
        symbolSize: ["80%", 30],
-     }],
+       symbolRepeat: true,
-   }}
+       symbolMargin: 2,
- />
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/PictorialBarDemo.tsx — Rounded Pictorial Bar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     xAxis: { type: "category", data: [...] },
+     xAxis: { type: "category", data: categories },
      yAxis: { type: "value" },
-     series: [{
+     tooltip: { trigger: "axis" },
-       type: "pictorialBar",
+     series: [{
-       data: [...],
+       type: "pictorialBar",
-       symbol: "roundRect",
+       data: values.map((v, i) => ({
-       symbolClip: true,
+         value: v,
-       symbolRepeat: true,
+         itemStyle: {
-       symbolSize: [30, 8],
+           color: ["#5470c6", "#91cc75", "#fac858", "#ee6666", "#73c0de"][i],
-     }],
+         },
-   }}
+       })),
- />
+       symbol: "roundRect",
+       barWidth: 30,
+       symbolClip: true,
+       symbolRepeat: true,
+       symbolSize: [30, 8],
+       symbolMargin: 2,
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/PieDemo.tsx — Basic Pie

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      tooltip: { trigger: "item" },
      legend: { top: "bottom" },
-     series: [{ type: "pie", radius: "60%", data: [...] }],
+     series: [{ type: "pie", radius: "60%", data: browserData }],
    }}
  />
```

### resources/js/react-demos/pages/echarts/PieDemo.tsx — Doughnut

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      tooltip: { trigger: "item" },
-     series: [{
+     legend: { top: "bottom" },
-       type: "pie",
+     series: [
-       radius: ["40%", "70%"],
+       {
-       data: [...],
+         type: "pie",
-       label: { show: true, formatter: "{b}: {d}%" },
+         radius: ["40%", "70%"],
-     }],
+         data: browserData,
-   }}
+         label: { show: true, formatter: "{b}: {d}%" },
- />
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/PieDemo.tsx — Rose Chart

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      tooltip: { trigger: "item" },
-     series: [{
+     legend: { top: "bottom" },
-       type: "pie",
+     series: [
-       radius: ["20%", "70%"],
+       {
-       roseType: "area",
+         type: "pie",
-       data: [...],
+         radius: ["20%", "70%"],
-     }],
+         roseType: "area",
-   }}
+         data: browserData,
- />
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/PieDemo.tsx — Nested Pie

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      tooltip: { trigger: "item" },
      series: [
-       { type: "pie", radius: [0, "30%"], data: [...] },
+       {
-       { type: "pie", radius: ["45%", "65%"], data: [...] },
+         type: "pie",
-     ],
+         radius: [0, "30%"],
-   }}
+         label: { position: "inner", fontSize: 12 },
- />
+         data: [
+           { value: 1548, name: "Desktop" },
+           { value: 1300, name: "Mobile" },
+         ],
+       },
+       {
+         type: "pie",
+         radius: ["45%", "65%"],
+         data: browserData,
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/RadarDemo.tsx — Basic Radar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     radar: { indicator: [{ name: "Sales", max: 6500 }, ...] },
+     tooltip: {},
-     series: [{
+     radar: { indicator: indicators },
-       type: "radar",
+     series: [{
-       data: [{ value: [4200, 3000, ...], name: "Budget" }],
+       type: "radar",
-     }],
+       data: [{ value: [4200, 3000, 20000, 35000, 50000, 18000], name: "Budget" }],
-   }}
+     }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/RadarDemo.tsx — Multi-Series Radar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     legend: {},
+     tooltip: {},
-     radar: { indicator: [...] },
+     legend: {},
-     series: [{
+     radar: { indicator: indicators },
-       type: "radar",
+     series: [{
-       data: [
+       type: "radar",
-         { value: [...], name: "Budget" },
+       data: [
-         { value: [...], name: "Spending" },
+         { value: [4200, 3000, 20000, 35000, 50000, 18000], name: "Budget" },
-       ],
+         { value: [5000, 14000, 28000, 26000, 42000, 21000], name: "Spending" },
-     }],
+       ],
-   }}
+     }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/RadarDemo.tsx — Filled Radar

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     radar: { indicator: [...], shape: "circle" },
+     tooltip: {},
-     series: [{
+     radar: { indicator: indicators, shape: "circle" },
-       type: "radar",
+     series: [{
-       areaStyle: { opacity: 0.3 },
+       type: "radar",
-       data: [
+       areaStyle: { opacity: 0.3 },
-         { value: [...], name: "Allocated" },
+       data: [
-         { value: [...], name: "Actual" },
+         { value: [4200, 3000, 20000, 35000, 50000, 18000], name: "Allocated" },
-       ],
+         { value: [5000, 14000, 28000, 26000, 42000, 21000], name: "Actual" },
-     }],
+       ],
-   }}
+     }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/SankeyDemo.tsx — Basic Sankey

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: { trigger: "item" },
-       type: "sankey",
+     series: [{
-       data: [{ name: "Source A" }, { name: "Process 1" }, ...],
+       type: "sankey",
-       links: [{ source: "Source A", target: "Process 1", value: 30 }, ...],
+       data: sankeyData.nodes,
-       emphasis: { focus: "adjacency" },
+       links: sankeyData.links,
-       lineStyle: { color: "gradient", curveness: 0.5 },
+       emphasis: { focus: "adjacency" },
-     }],
+       lineStyle: { color: "gradient", curveness: 0.5 },
-   }}
+     }],
-   style={{ height: 450 }}
+   }}
- />
+   style={{ height: 450 }}
+ />
```

### resources/js/react-demos/pages/echarts/SankeyDemo.tsx — Vertical Sankey

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: { trigger: "item" },
-       type: "sankey",
+     series: [{
-       orient: "vertical",
+       type: "sankey",
-       data: [...],
+       orient: "vertical",
-       links: [...],
+       data: sankeyData.nodes,
-       label: { position: "top" },
+       links: sankeyData.links,
-     }],
+       emphasis: { focus: "adjacency" },
-   }}
+       lineStyle: { color: "source", curveness: 0.5 },
-   style={{ height: 500 }}
+       label: { position: "top" },
- />
+     }],
+   }}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/Scatter3DDemo.tsx — Basic 3D Scatter

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
-     xAxis3D: { type: "value" },
+     tooltip: {},
-     yAxis3D: { type: "value" },
+     xAxis3D: { type: "value" },
-     zAxis3D: { type: "value" },
+     yAxis3D: { type: "value" },
-     grid3D: { viewControl: { autoRotate: true } },
+     zAxis3D: { type: "value" },
-     series: [{
+     grid3D: { viewControl: { autoRotate: true } },
-       type: "scatter3D",
+     series: [{
-       data: [[x, y, z], ...],
+       type: "scatter3D",
-       symbolSize: 5,
+       data: generateScatter3DData(200),
-     }],
+       symbolSize: 5,
-   }}
+       itemStyle: { opacity: 0.8 },
-   style={{ height: 500 }}
+     }],
- />
+   } as any}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/Scatter3DDemo.tsx — Multi-Group Scatter

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
-     legend: {},
+     tooltip: {},
-     xAxis3D: { type: "value" },
+     legend: {},
-     yAxis3D: { type: "value" },
+     xAxis3D: { type: "value" },
-     zAxis3D: { type: "value" },
+     yAxis3D: { type: "value" },
-     series: [
+     zAxis3D: { type: "value" },
-       { name: "Group A", type: "scatter3D", data: [...] },
+     grid3D: { viewControl: { distance: 200 } },
-       { name: "Group B", type: "scatter3D", data: [...] },
+     series: [
-     ],
+       { name: "Group A", type: "scatter3D", data: generateScatter3DData(80), symbolSize: 6 },
-   }}
+       { name: "Group B", type: "scatter3D", data: generateScatter3DData(80).map(d => [d[0] + 30, d[1] + 30, d[2]]), symbolSize: 6 },
-   style={{ height: 500 }}
+     ],
- />
+   } as any}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/ScatterDemo.tsx — Basic Scatter

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      xAxis: { type: "value" },
      yAxis: { type: "value" },
-     series: [{ type: "scatter", data: [[x, y], ...] }],
+     tooltip: { trigger: "item" },
-   }}
+     series: [{ type: "scatter", data: generateScatterData(50, 100, 100) }],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/ScatterDemo.tsx — Multi-Series

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      xAxis: { type: "value" },
      yAxis: { type: "value" },
-     legend: {},
+     tooltip: { trigger: "item" },
-     series: [
+     legend: {},
-       { name: "Group A", type: "scatter", data: [...] },
+     series: [
-       { name: "Group B", type: "scatter", data: [...] },
+       { name: "Group A", type: "scatter", data: generateScatterData(30, 50, 80) },
-     ],
+       { name: "Group B", type: "scatter", data: generateScatterData(30, 100, 60).map(d => [d[0] + 30, d[1] + 20]) },
-   }}
+     ],
- />
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/ScatterDemo.tsx — Bubble Chart

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      xAxis: { type: "value" },
      yAxis: { type: "value" },
-     series: [{
+     tooltip: {
-       type: "scatter",
+       trigger: "item",
-       symbolSize: (val) => val[2] * 2,
+       formatter: (params: any) => `${params.value[0]}, ${params.value[1]}<br/>Size: ${params.value[2]}`,
-       data: [[x, y, size], ...],
+     },
-     }],
+     series: [
-   }}
+       {
- />
+         type: "scatter",
+         symbolSize: (val: number[]) => val[2] * 2,
+         data: Array.from({ length: 30 }, () => [
+           +(Math.random() * 100).toFixed(1),
+           +(Math.random() * 100).toFixed(1),
+           +(Math.random() * 20 + 5).toFixed(0),
+         ]),
+       },
+     ],
+   }}
+ />
```

### resources/js/react-demos/pages/echarts/SunburstDemo.tsx — Basic Sunburst

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: {},
-       type: "sunburst",
+     series: [{
-       data: [
+       type: "sunburst",
-         { name: "Frontend", children: [
+       data: sunburstData,
-           { name: "React", value: 25 }, ...
+       radius: [0, "90%"],
-         ] },
+       label: { rotate: "radial" },
-         { name: "Backend", children: [...] },
+     }],
-       ],
+   }}
-       radius: [0, "90%"],
+   style={{ height: 500 }}
-       label: { rotate: "radial" },
+ />
-     }],
-   }}
-   style={{ height: 500 }}
- />
```

### resources/js/react-demos/pages/echarts/SunburstDemo.tsx — Styled Sunburst

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: {},
-       type: "sunburst",
+     series: [{
-       data: [...],
+       type: "sunburst",
-       radius: ["15%", "90%"],
+       data: sunburstData,
-       itemStyle: { borderRadius: 4, borderWidth: 2 },
+       radius: ["15%", "90%"],
-       emphasis: { focus: "ancestor" },
+       label: { rotate: "radial", fontSize: 11 },
-     }],
+       itemStyle: { borderRadius: 4, borderWidth: 2 },
-   }}
+       emphasis: { focus: "ancestor" },
-   style={{ height: 500 }}
+     }],
- />
+   }}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/SurfaceDemo.tsx — Mathematical Surface

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
-     xAxis3D: { type: "value" },
+     tooltip: {},
-     yAxis3D: { type: "value" },
+     xAxis3D: { type: "value", min: -3, max: 3 },
-     zAxis3D: { type: "value" },
+     yAxis3D: { type: "value", min: -3, max: 3 },
-     grid3D: { viewControl: { autoRotate: true } },
+     zAxis3D: { type: "value" },
-     series: [{
+     grid3D: { viewControl: { autoRotate: true, autoRotateSpeed: 5 } },
-       type: "surface",
+     visualMap: { show: false, min: -1, max: 1, inRange: { color: ["#313695", "#4575b4", "#74add1", "#abd9e9", "#fee090", "#fdae61", "#f46d43", "#d73027"] } },
-       parametric: true,
+     series: [{
-       wireframe: { show: true },
+       type: "surface",
-       parametricEquation: {
+       parametric: true,
-         u: { min: -Math.PI, max: Math.PI, step: Math.PI / 20 },
+       wireframe: { show: true },
-         v: { min: -Math.PI, max: Math.PI, step: Math.PI / 20 },
+       parametricEquation: {
-         x: (u, v) => Math.sin(u) * Math.cos(v),
+         u: { min: -Math.PI, max: Math.PI, step: Math.PI / 20 },
-         y: (u, v) => Math.sin(u) * Math.sin(v),
+         v: { min: -Math.PI, max: Math.PI, step: Math.PI / 20 },
-         z: (u) => Math.cos(u),
+         x: (u: number, v: number) => Math.sin(u) * Math.cos(v),
-       },
+         y: (u: number, v: number) => Math.sin(u) * Math.sin(v),
-     }],
+         z: (u: number, _v: number) => Math.cos(u),
-   }}
+       },
-   style={{ height: 500 }}
+     }],
- />
+   } as any}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/SurfaceDemo.tsx — Wavy Surface

```diff
--- code prop
+++ rendered JSX
  <EChart3D
    option={{
-     xAxis3D: { type: "value" },
+     tooltip: {},
-     yAxis3D: { type: "value" },
+     xAxis3D: { type: "value" },
-     zAxis3D: { type: "value" },
+     yAxis3D: { type: "value" },
-     series: [{
+     zAxis3D: { type: "value" },
-       type: "surface",
+     grid3D: { viewControl: { distance: 200 } },
-       equation: {
+     visualMap: { show: false, min: -1, max: 1, inRange: { color: ["#006edd", "#e0ffff"] } },
-         x: { min: -3, max: 3, step: 0.1 },
+     series: [{
-         y: { min: -3, max: 3, step: 0.1 },
+       type: "surface",
-         z: (x, y) => Math.sin(x * x + y * y),
+       equation: {
-       },
+         x: { min: -3, max: 3, step: 0.1 },
-       shading: "realistic",
+         y: { min: -3, max: 3, step: 0.1 },
-     }],
+         z: (x: number, y: number) => Math.sin(x * x + y * y) * Math.exp(-0.1 * (x * x + y * y)),
-   }}
+       },
-   style={{ height: 500 }}
+       wireframe: { show: false },
- />
+       shading: "realistic",
+     }],
+   } as any}
+   style={{ height: 500 }}
+ />
```

### resources/js/react-demos/pages/echarts/ThemeRiverDemo.tsx — Basic Theme River

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      tooltip: { trigger: "axis" },
      legend: {},
      singleAxis: { type: "time", bottom: 30 },
      series: [{
        type: "themeRiver",
-       data: [
+       data: themeRiverData,
-         ["2015/01", 10, "DL"],
+       emphasis: { focus: "self" },
-         ["2015/01", 15, "ML"],
+       label: { show: false },
-         ...
+     }],
-       ],
+   }}
-       emphasis: { focus: "self" },
+   style={{ height: 400 }}
-     }],
+ />
-   }}
-   style={{ height: 400 }}
- />
```

### resources/js/react-demos/pages/echarts/ThemeRiverDemo.tsx — Styled Theme River

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
      color: ["#5470c6", "#91cc75", "#fac858"],
-     singleAxis: { type: "time", bottom: 30 },
+     tooltip: { trigger: "axis" },
-     series: [{
+     legend: {},
-       type: "themeRiver",
+     singleAxis: { type: "time", bottom: 30 },
-       data: [...],
+     series: [{
-       emphasis: { focus: "self" },
+       type: "themeRiver",
-     }],
+       data: themeRiverData,
-   }}
+       emphasis: { focus: "self" },
-   style={{ height: 400 }}
+     }],
- />
+   }}
+   style={{ height: 400 }}
+ />
```

### resources/js/react-demos/pages/echarts/TreemapDemo.tsx — Basic Treemap

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: {},
-       type: "treemap",
+     series: [{
-       data: [
+       type: "treemap",
-         { name: "Technology", value: 100, children: [...] },
+       data: treemapData,
-         { name: "Design", value: 60, children: [...] },
+       label: { show: true, formatter: "{b}" },
-       ],
+       breadcrumb: { show: true },
-       label: { show: true, formatter: "{b}" },
+     }],
-       breadcrumb: { show: true },
+   }}
-     }],
+ />
-   }}
- />
```

### resources/js/react-demos/pages/echarts/TreemapDemo.tsx — Styled Treemap

```diff
--- code prop
+++ rendered JSX
  <EChart
    option={{
-     series: [{
+     tooltip: { formatter: "{b}: {c}" },
-       type: "treemap",
+     series: [{
-       data: [...],
+       type: "treemap",
-       levels: [
+       data: treemapData,
-         { itemStyle: { borderColor: "#555", borderWidth: 4 } },
+       levels: [
-         { colorSaturation: [0.3, 0.6] },
+         { itemStyle: { borderColor: "#555", borderWidth: 4, gapWidth: 4 } },
-       ],
+         { colorSaturation: [0.3, 0.6], itemStyle: { borderColorSaturation: 0.7, gapWidth: 2, borderWidth: 2 } },
-     }],
+         { colorSaturation: [0.3, 0.5], itemStyle: { borderColorSaturation: 0.6, gapWidth: 1 } },
-   }}
+       ],
- />
+     }],
+   }}
+ />
```

### resources/js/react-demos/pages/EditorDemo.tsx — Custom Toolbar with Actions

```diff
--- code prop
+++ rendered JSX
- function CustomToolbarButtons() {
+ <Editor placeholder="Custom toolbar editor...">
-   const { exec } = useEditor();
+   <Editor.Toolbar>
-   return (
+     <CustomToolbarButtons />
-     <>
+   </Editor.Toolbar>
-       <Action size="xs" variant="ghost" onClick={() => exec("bold")}>B</Action>
+   <Editor.Content />
-       <Editor.Toolbar.Separator />
+ </Editor>
-       <Action size="xs" variant="ghost" onClick={() => exec("formatBlock", "h1")}>H1</Action>
-     </>
-   );
- }
- 
- <Editor>
-   <Editor.Toolbar>
-     <CustomToolbarButtons />
-   </Editor.Toolbar>
-   <Editor.Content />
- </Editor>
```

### resources/js/react-demos/pages/EditorDemo.tsx — Render Extensions

```diff
--- code prop
+++ rendered JSX
- // Define extension components
+ <ContentRenderer
- function QuestionsRenderer({ content, attributes }: RenderExtensionProps) {
+   value={CONTENT_WITH_EXTENSIONS}
-   const questions = JSON.parse(content);
+   format="html"
-   return <div>...</div>;
+   extensions={DEMO_EXTENSIONS}
- }
+ />
- 
- // Register extensions
- const extensions: RenderExtension[] = [
-   { tag: "questions", component: QuestionsRenderer },
-   { tag: "thinking", component: ThinkingRenderer },
-   { tag: "alert",    component: AlertRenderer },
- ];
- 
- // Use in ContentRenderer
- <ContentRenderer
-   value={'<p>Text</p><questions>[...]</questions>'}
-   extensions={extensions}
- />
- 
- // Use in Editor
- <Editor extensions={extensions}>
-   <Editor.Toolbar />
-   <Editor.Content />
- </Editor>
- 
- // Or register globally (applies to all instances)
- registerExtension({ tag: "questions", component: QuestionsRenderer });
```

### resources/js/react-demos/pages/EditorDemo.tsx — ContentRenderer

```diff
--- code prop
+++ rendered JSX
- <ContentRenderer value={markdownString} format="markdown" />
+ <div>
- <ContentRenderer value={htmlString} format="html" />
+   <p className="mb-2 text-xs font-medium text-zinc-500">format=&quot;markdown&quot;</p>
- <ContentRenderer value={content} format="auto" />
+   <div className="rounded-lg border border-zinc-200 p-4 dark:border-zinc-700">
+     <ContentRenderer value={SAMPLE_MARKDOWN} format="markdown" />
+   </div>
+ </div>
+ <div>
+   <p className="mb-2 text-xs font-medium text-zinc-500">format=&quot;html&quot;</p>
+   <div className="rounded-lg border border-zinc-200 p-4 dark:border-zinc-700">
+     <ContentRenderer value={SAMPLE_HTML} format="html" />
+   </div>
+ </div>
```

### resources/js/react-demos/pages/EmojiDemo.tsx — By Name

```diff
--- code prop
+++ rendered JSX
- <Emoji name="rocket" size="xl" />
+ <Emoji name="grinning face" size="xl" />
+ <Emoji name="thumbs up" size="xl" />
+ <Emoji name="rocket" size="xl" />
+ <Emoji name="heart" size="xl" />
```

### resources/js/react-demos/pages/EmojiDemo.tsx — Direct Emoji

```diff
--- code prop
+++ rendered JSX
- <Emoji emoji="🎉" size="lg" />
+ <Emoji emoji="🎉" size="sm" />
+ <Emoji emoji="🎉" size="md" />
+ <Emoji emoji="🎉" size="lg" />
+ <Emoji emoji="🎉" size="xl" />
```

### resources/js/react-demos/pages/FileUploadDemo.tsx — Basic

```diff
--- code prop
+++ rendered JSX
  <FileUpload value={files} onChange={setFiles} maxFiles={5}>
    <FileUpload.Dropzone />
    <FileUpload.List />
  </FileUpload>
+ <p className="mt-2 text-sm text-zinc-500">{files.length} file(s) selected</p>
```

### resources/js/react-demos/pages/HeadingDemo.tsx — Levels

```diff
--- code prop
+++ rendered JSX
- <Heading as="h1" size="2xl">Heading 1</Heading>
+ <Heading as="h1" size="2xl">Heading 1 (2xl)</Heading>
- <Heading as="h2" size="xl">Heading 2</Heading>
+ <Heading as="h2" size="xl">Heading 2 (xl)</Heading>
- <Heading as="h3" size="lg">Heading 3</Heading>
+ <Heading as="h3" size="lg">Heading 3 (lg)</Heading>
+ <Heading as="h4" size="md">Heading 4 (md)</Heading>
+ <Heading as="h5" size="sm">Heading 5 (sm)</Heading>
+ <Heading as="h6" size="xs">Heading 6 (xs)</Heading>
```

### resources/js/react-demos/pages/HeadingDemo.tsx — Weights

```diff
--- code prop
+++ rendered JSX
- <Heading weight="semibold">Semibold weight</Heading>
+ <Heading weight="normal">Normal weight</Heading>
- <Heading weight="bold">Bold weight</Heading>
+ <Heading weight="medium">Medium weight</Heading>
+ <Heading weight="semibold">Semibold weight</Heading>
+ <Heading weight="bold">Bold weight</Heading>
```

### resources/js/react-demos/pages/IconDemo.tsx — Custom Icon Set

```diff
--- code prop
+++ rendered JSX
- import { registerIconSet } from "@particle-academy/react-fancy";
+ <div className="flex flex-col items-center gap-1.5">
- import type { IconSet } from "@particle-academy/react-fancy";
+   <Icon name="checkmark" iconSet="demo" size="lg" className="text-emerald-500" />
- 
+   <span className="text-[10px] text-zinc-400">checkmark</span>
- const myIcons: IconSet = {
+ </div>
-   resolve: (name) => {
+ <div className="flex flex-col items-center gap-1.5">
-     const icons = {
+   <Icon name="cross" iconSet="demo" size="lg" className="text-rose-500" />
-       checkmark: ({ size }) => <svg>...</svg>,
+   <span className="text-[10px] text-zinc-400">cross</span>
-       cross: ({ size }) => <svg>...</svg>,
+ </div>
-     };
+ <div className="flex flex-col items-center gap-1.5">
-     return icons[name] ?? null;
+   <Icon name="flame" iconSet="demo" size="lg" className="text-orange-500" />
-   },
+   <span className="text-[10px] text-zinc-400">flame</span>
- };
+ </div>
- 
- registerIconSet("my-set", myIcons);
- 
- <Icon name="checkmark" iconSet="my-set" />
- <Icon name="cross" iconSet="my-set" />
```

### resources/js/react-demos/pages/IconDemo.tsx — Inside Input Fields

```diff
--- code prop
+++ rendered JSX
  <Input
    leading={<Icon name="search" size="sm" />}
    placeholder="Search..."
- />
+   value={search}
- <Input
+   onChange={(e) => setSearch(e.target.value)}
-   leading={<Icon name="mail" size="sm" />}
+ />
-   trailing={<Icon name="check" size="sm" className="text-emerald-500" />}
+ <Input
-   placeholder="Email address"
+   leading={<Icon name="mail" size="sm" />}
- />
+   trailing={<Icon name="check" size="sm" className="text-emerald-500" />}
+   placeholder="Email address"
+ />
+ <Input
+   leading={<Icon name="lock" size="sm" />}
+   placeholder="Password"
+   type="password"
+ />
```

### resources/js/react-demos/pages/IconDemo.tsx — Inside Timeline

```diff
--- code prop
+++ rendered JSX
  <Timeline>
    <Timeline.Item icon={<Icon name="git-commit" />} color="blue">
-     Initial commit
+     <span className="font-medium">Initial commit</span>
-   </Timeline.Item>
+     <span className="block text-sm text-zinc-500">Set up project scaffolding</span>
-   <Timeline.Item icon={<Icon name="git-branch" />} color="purple">
+   </Timeline.Item>
-     Feature branch created
+   <Timeline.Item icon={<Icon name="git-branch" />} color="purple">
-   </Timeline.Item>
+     <span className="font-medium">Feature branch created</span>
-   <Timeline.Item icon={<Icon name="message-square" />} color="amber">
+     <span className="block text-sm text-zinc-500">feat/icon-improvements</span>
-     Code review requested
+   </Timeline.Item>
-   </Timeline.Item>
+   <Timeline.Item icon={<Icon name="message-square" />} color="amber">
-   <Timeline.Item icon={<Icon name="check-circle" />} color="green">
+     <span className="font-medium">Code review requested</span>
-     Merged to main
+     <span className="block text-sm text-zinc-500">2 reviewers assigned</span>
    </Timeline.Item>
- </Timeline>
+   <Timeline.Item icon={<Icon name="check-circle" />} color="green">
+     <span className="font-medium">Merged to main</span>
+     <span className="block text-sm text-zinc-500">All checks passed</span>
+   </Timeline.Item>
+ </Timeline>
```

### resources/js/react-demos/pages/InputsDemo.tsx — Input

```diff
--- code prop
+++ rendered JSX
- <Input label="Full name" placeholder="John Doe" value={value} onValueChange={setValue} />
+ <Input
- <Input label="Email" type="email" placeholder="john@example.com" />
+   label="Full name"
- <Input label="Password" type="password" placeholder="••••••••" />
+   placeholder="John Doe"
- <Input label="Search" type="search" placeholder="Search..." leading={<SearchIcon />} />
+   dirty={dirty}
+   value={textValue}
+   onValueChange={setTextValue}
+ />
+ <Input
+   label="Email"
+   type="email"
+   placeholder="john@example.com"
+   dirty={dirty}
+ />
+ <Input
+   label="Password"
+   type="password"
+   placeholder="••••••••"
+   dirty={dirty}
+ />
+ <Input
+   label="Search"
+   type="search"
+   placeholder="Search..."
+   dirty={dirty}
+   leading={<SearchIcon />}
+ />
```

### resources/js/react-demos/pages/InputsDemo.tsx — Input — Error State

```diff
--- code prop
+++ rendered JSX
- <Input label="Email" type="email" value="not-an-email" error="Please enter a valid email address" />
+ <Input
- <Input label="Username" value="ab" error="Username must be at least 3 characters" required />
+   label="Email"
+   type="email"
+   value="not-an-email"
+   error="Please enter a valid email address"
+ />
+ <Input
+   label="Username"
+   value="ab"
+   error="Username must be at least 3 characters"
+   required
+ />
```

### resources/js/react-demos/pages/InputsDemo.tsx — Textarea

```diff
--- code prop
+++ rendered JSX
- <Textarea label="Bio" placeholder="Tell us about yourself..." minRows={3} />
+ <Textarea
- <Textarea label="Auto-resize" placeholder="Type more lines..." autoResize minRows={3} maxRows={8} />
+   label="Bio"
+   placeholder="Tell us about yourself..."
+   dirty={dirty}
+   value={textareaValue}
+   onValueChange={setTextareaValue}
+   minRows={3}
+ />
+ <Textarea
+   label="Auto-resize"
+   placeholder="Type more lines and watch me grow..."
+   autoResize
+   dirty={dirty}
+   minRows={3}
+   maxRows={8}
+ />
```

### resources/js/react-demos/pages/InputsDemo.tsx — Checkbox

```diff
--- code prop
+++ rendered JSX
- <Checkbox label="Accept terms" description="You agree to our terms." checked={checked} onCheckedChange={setChecked} />
+ <Checkbox
- <Checkbox label="Indeterminate" indeterminate />
+   label="Accept terms and conditions"
- <Checkbox label="Disabled" disabled defaultChecked />
+   description="You agree to our terms of service and privacy policy."
+   checked={checkboxChecked}
+   onCheckedChange={setCheckboxChecked}
+   dirty={dirty}
+ />
+ <Checkbox
+   label="Indeterminate state"
+   indeterminate
+   dirty={dirty}
+ />
+ <Checkbox
+   label="Disabled"
+   disabled
+   defaultChecked
+ />
```

### resources/js/react-demos/pages/InputsDemo.tsx — Switch

```diff
--- code prop
+++ rendered JSX
- <Switch label="Notifications" checked={checked} onCheckedChange={setChecked} />
+ <Switch
- <Switch label="Blue" color="blue" defaultChecked />
+   label="Email notifications"
- <Switch size="sm" label="SM" defaultChecked />
+   description="Receive email notifications for updates"
+   checked={switchChecked}
+   onCheckedChange={setSwitchChecked}
+   dirty={dirty}
+ />
+ <div className="flex flex-wrap gap-4">
+   <Switch label="Blue" color="blue" defaultChecked />
+   <Switch label="Green" color="green" defaultChecked />
+   <Switch label="Red" color="red" defaultChecked />
+   <Switch label="Purple" color="purple" defaultChecked />
+   <Switch label="Amber" color="amber" defaultChecked />
+ </div>
+ <div className="flex flex-wrap items-center gap-4">
+   <Switch size="xs" label="XS" defaultChecked />
+   <Switch size="sm" label="SM" defaultChecked />
+   <Switch size="md" label="MD" defaultChecked />
+   <Switch size="lg" label="LG" defaultChecked />
+   <Switch size="xl" label="XL" defaultChecked />
+ </div>
```

### resources/js/react-demos/pages/InputsDemo.tsx — DatePicker

```diff
--- code prop
+++ rendered JSX
- <DatePicker label="Start date" value={date} onValueChange={setDate} />
+ <DatePicker
- <DatePicker label="With time" includeTime />
+   label="Start date"
- <DatePicker label="Date range" range value={range} onValueChange={setRange} />
+   dirty={dirty}
+   value={dateValue}
+   onValueChange={setDateValue}
+ />
+ <DatePicker
+   label="With time"
+   dirty={dirty}
+   includeTime
+ />
+ <div className="sm:col-span-2">
+   <DatePicker
+     label="Date range"
+     dirty={dirty}
+     range
+     value={dateRangeValue}
+     onValueChange={setDateRangeValue}
+   />
+ </div>
```

### resources/js/react-demos/pages/InputsDemo.tsx — Field (standalone)

```diff
--- code prop
+++ rendered JSX
- <Field label="Custom field" description="You can wrap any content." required>
+ <Field label="Custom field" description="You can wrap any content in a Field." required>
-   <div>Custom content goes here</div>
+   <div className="rounded-lg border border-dashed border-zinc-300 p-4 text-center text-sm text-zinc-500 dark:border-zinc-600 dark:text-zinc-400">
- </Field>
+     Custom content goes here
+   </div>
+ </Field>
```

### resources/js/react-demos/pages/MobileMenuDemo.tsx — Flyout Menu (Right)

```diff
--- code prop
+++ rendered JSX
- <MobileMenu.Flyout open={open} onClose={close} side="right" title="Menu">
+ <button
-   ...
+   onClick={() => setFlyoutRightOpen(true)}
- </MobileMenu.Flyout>
+   className="inline-flex items-center gap-2 rounded-md bg-zinc-900 px-4 py-2 text-sm font-medium text-white transition-colors hover:bg-zinc-800 dark:bg-zinc-100 dark:text-zinc-900 dark:hover:bg-zinc-200"
+ >
+   <Icon name="menu" size="sm" />
+   Open Right Flyout
+ </button>
+ 
+ <MobileMenu.Flyout open={flyoutRightOpen} onClose={() => setFlyoutRightOpen(false)} side="right" title="Account">
+   <MobileMenu.Item icon={<Icon name="user" size="sm" />}>Profile</MobileMenu.Item>
+   <MobileMenu.Item icon={<Icon name="settings" size="sm" />}>Settings</MobileMenu.Item>
+   <MobileMenu.Item icon={<Icon name="bell" size="sm" />} badge={<Badge size="xs" color="red">2</Badge>}>
+     Notifications
+   </MobileMenu.Item>
+   <MobileMenu.Item icon={<Icon name="help-circle" size="sm" />}>Help</MobileMenu.Item>
+   <MobileMenu.Item icon={<Icon name="log-out" size="sm" />}>Sign out</MobileMenu.Item>
+ </MobileMenu.Flyout>
```

### resources/js/react-demos/pages/ModalDemo.tsx — Basic

```diff
--- code prop
+++ rendered JSX
- <Action onClick={() => setOpen(true)}>Open Modal</Action>
+ <Action onClick={() => setBasicOpen(true)}>Open Modal</Action>
- <Modal open={open} onClose={() => setOpen(false)}>
+ <Modal open={basicOpen} onClose={() => setBasicOpen(false)}>
-   <Modal.Header><h2>Confirm Action</h2></Modal.Header>
+   <Modal.Header>
-   <Modal.Body><p>Are you sure?</p></Modal.Body>
+     <h2 className="text-lg font-semibold">Confirm Action</h2>
-   <Modal.Footer>
+   </Modal.Header>
-     <Action size="sm" onClick={() => setOpen(false)}>Cancel</Action>
+   <Modal.Body>
-     <Action size="sm" onClick={() => setOpen(false)}>Confirm</Action>
+     <p className="text-sm text-zinc-600 dark:text-zinc-400">
-   </Modal.Footer>
+       Are you sure you want to proceed? This action cannot be undone.
- </Modal>
+     </p>
+   </Modal.Body>
+   <Modal.Footer>
+     <div className="flex justify-end gap-2">
+       <Action size="sm" onClick={() => setBasicOpen(false)}>
+         Cancel
+       </Action>
+       <Action size="sm" onClick={() => setBasicOpen(false)}>
+         Confirm
+       </Action>
+     </div>
+   </Modal.Footer>
+ </Modal>
```

### resources/js/react-demos/pages/ModalDemo.tsx — Large Size

```diff
--- code prop
+++ rendered JSX
- <Modal open={open} onClose={() => setOpen(false)} size="lg">
+ <Action onClick={() => setLgOpen(true)}>Open Large Modal</Action>
-   <Modal.Header><h2>Project Details</h2></Modal.Header>
+ <Modal open={lgOpen} onClose={() => setLgOpen(false)} size="lg">
-   <Modal.Body><p>More complex content here.</p></Modal.Body>
+   <Modal.Header>
-   <Modal.Footer>
+     <h2 className="text-lg font-semibold">Project Details</h2>
-     <Action size="sm" onClick={() => setOpen(false)}>Close</Action>
+   </Modal.Header>
-   </Modal.Footer>
+   <Modal.Body>
- </Modal>
+     <div className="space-y-4">
+       <p className="text-sm text-zinc-600 dark:text-zinc-400">
+         This is a larger modal that can hold more complex content such as forms,
+         tables, or multi-step wizards.
+       </p>
+       <div className="h-32 rounded-lg bg-zinc-100 dark:bg-zinc-800" />
+       <p className="text-sm text-zinc-600 dark:text-zinc-400">
+         Press Escape or click the backdrop to close.
+       </p>
+     </div>
+   </Modal.Body>
+   <Modal.Footer>
+     <div className="flex justify-end">
+       <Action size="sm" onClick={() => setLgOpen(false)}>Close</Action>
+     </div>
+   </Modal.Footer>
+ </Modal>
```

### resources/js/react-demos/pages/NavbarDemo.tsx — Basic

```diff
--- code prop
+++ rendered JSX
- <Navbar>
+ <Navbar className="rounded-lg">
-   <Navbar.Brand><span>MyApp</span></Navbar.Brand>
+   <Navbar.Brand>
-   <Navbar.Items>
+     <span className="text-lg font-bold">MyApp</span>
-     <Navbar.Item href="#">Home</Navbar.Item>
+   </Navbar.Brand>
-     <Navbar.Item href="#">About</Navbar.Item>
+   <Navbar.Items>
-   </Navbar.Items>
+     <Navbar.Item href="#">Home</Navbar.Item>
- </Navbar>
+     <Navbar.Item href="#">About</Navbar.Item>
+     <Navbar.Item href="#">Services</Navbar.Item>
+     <Navbar.Item href="#">Contact</Navbar.Item>
+   </Navbar.Items>
+ </Navbar>
```

### resources/js/react-demos/pages/NavbarDemo.tsx — With Toggle

```diff
--- code prop
+++ rendered JSX
- <Navbar>
+ <Navbar className="rounded-lg">
-   <Navbar.Brand><span>Dashboard</span></Navbar.Brand>
+   <Navbar.Brand>
-   <Navbar.Toggle />
+     <span className="text-lg font-bold">Dashboard</span>
-   <Navbar.Items>
+   </Navbar.Brand>
-     <Navbar.Item href="#">Overview</Navbar.Item>
+   <Navbar.Toggle />
-     <Navbar.Item href="#">Settings</Navbar.Item>
+   <Navbar.Items>
-   </Navbar.Items>
+     <Navbar.Item href="#">Overview</Navbar.Item>
- </Navbar>
+     <Navbar.Item href="#">Analytics</Navbar.Item>
+     <Navbar.Item href="#">Settings</Navbar.Item>
+   </Navbar.Items>
+ </Navbar>
```

### resources/js/react-demos/pages/NestedCarouselDemo.tsx — Independent Nested Carousels

```diff
--- code prop
+++ rendered JSX
- <Carousel>
+ <Carousel className="max-w-2xl">
    <Carousel.Panels>
-     <Carousel.Slide>Outer Slide 1</Carousel.Slide>
+     {/* Outer Slide 1 */}
      <Carousel.Slide>
-       {/* Nested carousel */}
+       <div className="flex h-56 items-center justify-center rounded-lg bg-blue-100 dark:bg-blue-900/40">
-       <Carousel>
+         <div className="text-center">
-         <Carousel.Panels>
+           <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-200">
-           <Carousel.Slide>Inner A</Carousel.Slide>
+             Outer Slide 1
-           <Carousel.Slide>Inner B</Carousel.Slide>
+           </h3>
-         </Carousel.Panels>
+           <p className="mt-1 text-sm text-blue-600 dark:text-blue-300">
-         <Carousel.Steps />
+             A simple slide in the outer carousel.
-         <Carousel.Controls />
+           </p>
-       </Carousel>
+         </div>
-     </Carousel.Slide>
+       </div>
-   </Carousel.Panels>
+     </Carousel.Slide>
-   <Carousel.Steps />
+ 
-   <Carousel.Controls />
+     {/* Outer Slide 2 — contains nested carousel */}
- </Carousel>
+     <Carousel.Slide>
+       <div className="rounded-lg bg-emerald-50 p-4 dark:bg-emerald-900/20">
+         <h3 className="mb-3 text-lg font-semibold text-emerald-800 dark:text-emerald-200">
+           Outer Slide 2 — Nested Carousel
+         </h3>
+ 
+         {/* Inner carousel */}
+         <div className="rounded-lg border border-emerald-200 bg-white p-4 dark:border-emerald-700 dark:bg-zinc-900">
+           <p className="mb-3 text-sm font-medium text-zinc-600 dark:text-zinc-400">
+             Inner Carousel
+           </p>
+           <Carousel>
+             <Carousel.Panels>
+               <Carousel.Slide>
+                 <div className="flex h-32 items-center justify-center rounded-lg bg-purple-100 text-purple-800 dark:bg-purple-900/40 dark:text-purple-200">
+                   Inner Slide A
+                 </div>
+               </Carousel.Slide>
+               <Carousel.Slide>
+                 <div className="flex h-32 items-center justify-center rounded-lg bg-pink-100 text-pink-800 dark:bg-pink-900/40 dark:text-pink-200">
+                   Inner Slide B
+                 </div>
+               </Carousel.Slide>
+               <Carousel.Slide>
+                 <div className="flex h-32 items-center justify-center rounded-lg bg-indigo-100 text-indigo-800 dark:bg-indigo-900/40 dark:text-indigo-200">
+                   Inner Slide C
+                 </div>
+               </Carousel.Slide>
+             </Carousel.Panels>
+             <div className="mt-3 flex items-center justify-between">
+               <Carousel.Steps />
+               <Carousel.Controls />
+             </div>
+           </Carousel>
+         </div>
+       </div>
+     </Carousel.Slide>
+ 
+     {/* Outer Slide 3 */}
+     <Carousel.Slide>
+       <div className="flex h-56 items-center justify-center rounded-lg bg-amber-100 dark:bg-amber-900/40">
+         <div className="text-center">
+           <h3 className="text-lg font-semibold text-amber-800 dark:text-amber-200">
+             Outer Slide 3
+           </h3>
+           <p className="mt-1 text-sm text-amber-600 dark:text-amber-300">
+             Back to a normal outer slide.
+           </p>
+         </div>
+       </div>
+     </Carousel.Slide>
+   </Carousel.Panels>
+ 
+   <div className="mt-4 flex items-center justify-between">
+     <Carousel.Steps />
+     <Carousel.Controls />
+   </div>
+ </Carousel>
```

### resources/js/react-demos/pages/PaginationDemo.tsx — Basic

```diff
--- code prop
+++ rendered JSX
- <Pagination page={page} onPageChange={setPage} totalPages={5} />
+ <Pagination page={page1} onPageChange={setPage1} totalPages={5} />
+ <p className="mt-2 text-sm text-zinc-500">Current page: {page1}</p>
```

### resources/js/react-demos/pages/PaginationDemo.tsx — Many Pages

```diff
--- code prop
+++ rendered JSX
- <Pagination page={page} onPageChange={setPage} totalPages={20} />
+ <Pagination page={page2} onPageChange={setPage2} totalPages={20} />
+ <p className="mt-2 text-sm text-zinc-500">Current page: {page2}</p>
```

### resources/js/react-demos/pages/PaginationDemo.tsx — Custom Siblings

```diff
--- code prop
+++ rendered JSX
- <Pagination page={page} onPageChange={setPage} totalPages={20} siblingCount={2} />
+ <Pagination page={page2} onPageChange={setPage2} totalPages={20} siblingCount={2} />
```

### resources/js/react-demos/pages/PillboxDemo.tsx — Max Items

```diff
--- code prop
+++ rendered JSX
- <Pillbox value={skills} onChange={setSkills} maxItems={5} placeholder="Add up to 5 skills..." />
+ <Pillbox
+   value={skills}
+   onChange={setSkills}
+   maxItems={5}
+   placeholder="Add up to 5 skills..."
+ />
+ <p className="mt-2 text-sm text-zinc-500">{skills.length}/5 skills added</p>
```

### resources/js/react-demos/pages/PopoverDemo.tsx — Basic

```diff
--- code prop
+++ rendered JSX
  <Popover>
    <Popover.Trigger>
      <Action>Open Popover</Action>
    </Popover.Trigger>
    <Popover.Content>
-     <p>Popover content here</p>
+     <div className="w-64">
-   </Popover.Content>
+       <h3 className="font-semibold mb-2">Popover Title</h3>
- </Popover>
+       <p className="text-sm text-zinc-500 dark:text-zinc-400">
+         This is a popover with arbitrary content. Click outside to close.
+       </p>
+     </div>
+   </Popover.Content>
+ </Popover>
```

### resources/js/react-demos/pages/PopoverDemo.tsx — Hover

```diff
--- code prop
+++ rendered JSX
  <Popover hover>
    <Popover.Trigger>
      <Action>Hover me</Action>
    </Popover.Trigger>
    <Popover.Content>
-     <p>Hoverable content</p>
+     <div className="w-56">
-   </Popover.Content>
+       <h3 className="font-semibold mb-1">Hover Popover</h3>
- </Popover>
+       <p className="text-sm text-zinc-500 dark:text-zinc-400">
+         Move your mouse here — the popover stays open.
+       </p>
+     </div>
+   </Popover.Content>
+ </Popover>
+ 
+ <Popover hover placement="right">
+   <Popover.Trigger>
+     <Action>Details</Action>
+   </Popover.Trigger>
+   <Popover.Content>
+     <div className="w-48">
+       <p className="text-sm text-zinc-500 dark:text-zinc-400">
+         Additional info shown on hover.
+       </p>
+     </div>
+   </Popover.Content>
+ </Popover>
```

### resources/js/react-demos/pages/ProfileDemo.tsx — Basic

```diff
--- code prop
+++ rendered JSX
- <Profile src="https://i.pravatar.cc/150" name="Jane Cooper" subtitle="Software Engineer" />
+ <Profile
+   src="https://i.pravatar.cc/150?u=jane"
+   name="Jane Cooper"
+   subtitle="Software Engineer"
+ />
```

### resources/js/react-demos/pages/ProfileDemo.tsx — With Status

```diff
--- code prop
+++ rendered JSX
- <Profile src="..." name="Alice Johnson" subtitle="Online now" status="online" />
+ <Profile
- <Profile src="..." name="Bob Smith" subtitle="Away" status="away" />
+   src="https://i.pravatar.cc/150?u=alice"
- <Profile fallback="CD" name="Carol Davis" subtitle="Busy" status="busy" />
+   name="Alice Johnson"
+   subtitle="Online now"
+   status="online"
+ />
+ <Profile
+   src="https://i.pravatar.cc/150?u=bob"
+   name="Bob Smith"
+   subtitle="Away for lunch"
+   status="away"
+ />
+ <Profile
+   fallback="CD"
+   name="Carol Davis"
+   subtitle="In a meeting"
+   status="busy"
+ />
```

### resources/js/react-demos/pages/ProgressDemo.tsx — Bar

```diff
--- code prop
+++ rendered JSX
- <Progress value={65} showValue />
+ <Progress value={value} showValue />
+ <input
+   type="range"
+   min={0}
+   max={100}
+   value={value}
+   onChange={(e) => setValue(Number(e.target.value))}
+   className="w-full"
+ />
```

### resources/js/react-demos/pages/ProgressDemo.tsx — Colors

```diff
--- code prop
+++ rendered JSX
  <Progress value={80} color="blue" />
  <Progress value={60} color="green" />
  <Progress value={45} color="amber" />
  <Progress value={30} color="red" />
+ <Progress value={70} color="violet" />
```

### resources/js/react-demos/pages/ProgressDemo.tsx — Circular

```diff
--- code prop
+++ rendered JSX
- <Progress variant="circular" value={75} size="lg" showValue color="green" />
+ <Progress variant="circular" value={25} size="sm" showValue />
+ <Progress variant="circular" value={50} size="md" showValue />
+ <Progress variant="circular" value={75} size="lg" showValue color="green" />
```

### resources/js/react-demos/pages/ProgressDemo.tsx — Indeterminate

```diff
--- code prop
+++ rendered JSX
- <Progress indeterminate />
+ <Progress indeterminate className="max-w-md" />
  <Progress variant="circular" indeterminate size="md" />
```

### resources/js/react-demos/pages/SelectDemo.tsx — Listbox (Single)

```diff
--- code prop
+++ rendered JSX
- <Select variant="listbox" list={roles} placeholder="Assign role..." />
+ <Select variant="listbox" label="Role" list={roles} placeholder="Assign role..." />
```

### resources/js/react-demos/pages/SelectDemo.tsx — Multi-Select with Checkboxes

```diff
--- code prop
+++ rendered JSX
- <Select multiple indicator="checkbox" list={roles} />
+ <Select
+   multiple
+   indicator="checkbox"
+   label="Permissions"
+   list={roles}
+   placeholder="Select permissions..."
+ />
```

### resources/js/react-demos/pages/SeparatorDemo.tsx — Horizontal

```diff
--- code prop
+++ rendered JSX
- <Separator />
+ <p className="text-sm text-zinc-600">Content above</p>
+ <Separator className="my-4" />
+ <p className="text-sm text-zinc-600">Content below</p>
```

### resources/js/react-demos/pages/SeparatorDemo.tsx — With Label

```diff
--- code prop
+++ rendered JSX
- <Separator label="OR" />
+ <Separator label="OR" className="my-4" />
+ <Separator label="Section Break" className="my-4" />
```

### resources/js/react-demos/pages/SeparatorDemo.tsx — Vertical

```diff
--- code prop
+++ rendered JSX
- <Separator orientation="vertical" />
+ <span className="text-sm">Left</span>
+ <Separator orientation="vertical" />
+ <span className="text-sm">Center</span>
+ <Separator orientation="vertical" />
+ <span className="text-sm">Right</span>
```

### resources/js/react-demos/pages/SidebarDemo.tsx — Collapsible Sidebar

```diff
--- code prop
+++ rendered JSX
- <Sidebar collapseMode="icons">
+ <SidebarExample collapseMode="icons" label={'collapseMode="icons" (default)'} />
-   <Sidebar.Toggle />
+ <SidebarExample collapseMode="letters" label={'collapseMode="letters"'} />
-   <Sidebar.Group label="Main">
-     <Sidebar.Item icon={<Icon name="home" size="sm" />} active>Dashboard</Sidebar.Item>
-     <Sidebar.Item icon={<Icon name="mail" size="sm" />} badge={<Badge>5</Badge>}>Inbox</Sidebar.Item>
-   </Sidebar.Group>
-   <Sidebar.Submenu label="Settings" icon={<Icon name="settings" size="sm" />} defaultOpen>
-     <Sidebar.Item>General</Sidebar.Item>
-     <Sidebar.Item>Security</Sidebar.Item>
-   </Sidebar.Submenu>
- </Sidebar>
```

### resources/js/react-demos/pages/SkeletonDemo.tsx — Card Skeleton

```diff
--- code prop
+++ rendered JSX
  <Skeleton shape="circle" width={48} />
- <Skeleton shape="text" width="70%" />
+ <div className="flex-1 space-y-2">
- <Skeleton shape="text" />
+   <Skeleton shape="text" width="70%" />
- <Skeleton shape="text" width="40%" />
+   <Skeleton shape="text" />
+   <Skeleton shape="text" width="40%" />
+ </div>
```

### resources/js/react-demos/pages/SpreadsheetDemo.tsx — Full Spreadsheet

```diff
--- code prop
+++ rendered JSX
- <Spreadsheet data={workbook} onChange={setWorkbook}>
+ <Spreadsheet
-   <Spreadsheet.Toolbar />
+   data={data}
-   <Spreadsheet.Grid />
+   onChange={setData}
-   <Spreadsheet.SheetTabs />
+   columnCount={10}
- </Spreadsheet>
+   rowCount={25}
+ >
+   <Spreadsheet.Toolbar />
+   <Spreadsheet.Grid />
+   <Spreadsheet.SheetTabs />
+ </Spreadsheet>
```

### resources/js/react-demos/pages/TabsDemo.tsx — Underline Variant

```diff
--- code prop
+++ rendered JSX
  <Tabs defaultTab="overview" variant="underline">
    <Tabs.List>
      <Tabs.Tab value="overview">Overview</Tabs.Tab>
      <Tabs.Tab value="features">Features</Tabs.Tab>
-   </Tabs.List>
+     <Tabs.Tab value="pricing">Pricing</Tabs.Tab>
-   <Tabs.Panels>
+   </Tabs.List>
-     <Tabs.Panel value="overview">Overview content</Tabs.Panel>
+   <Tabs.Panels>
-     <Tabs.Panel value="features">Features content</Tabs.Panel>
+     <Tabs.Panel value="overview">
-   </Tabs.Panels>
+       <p className="py-4 text-sm text-zinc-600 dark:text-zinc-400">
- </Tabs>
+         Welcome to the overview tab. This provides a high-level summary of the product.
+       </p>
+     </Tabs.Panel>
+     <Tabs.Panel value="features">
+       <p className="py-4 text-sm text-zinc-600 dark:text-zinc-400">
+         Explore the key features that make this product stand out.
+       </p>
+     </Tabs.Panel>
+     <Tabs.Panel value="pricing">
+       <p className="py-4 text-sm text-zinc-600 dark:text-zinc-400">
+         Choose a plan that fits your needs starting at $9/month.
+       </p>
+     </Tabs.Panel>
+   </Tabs.Panels>
+ </Tabs>
```

### resources/js/react-demos/pages/TabsDemo.tsx — Pills Variant (Segmented Control)

```diff
--- code prop
+++ rendered JSX
- <Tabs defaultTab="general" variant="pills">
+ <Tabs defaultTab="tab1" variant="pills">
    <Tabs.List>
-     <Tabs.Tab value="general">General</Tabs.Tab>
+     <Tabs.Tab value="tab1">General</Tabs.Tab>
-     <Tabs.Tab value="security">Security</Tabs.Tab>
+     <Tabs.Tab value="tab2">Security</Tabs.Tab>
-     <Tabs.Tab value="notifications">Notifications</Tabs.Tab>
+     <Tabs.Tab value="tab3">Notifications</Tabs.Tab>
    </Tabs.List>
    <Tabs.Panels>
-     <Tabs.Panel value="general">General content</Tabs.Panel>
+     <Tabs.Panel value="tab1">
-     <Tabs.Panel value="security">Security content</Tabs.Panel>
+       <p className="py-4 text-sm text-zinc-600 dark:text-zinc-400">General settings panel content.</p>
-   </Tabs.Panels>
+     </Tabs.Panel>
- </Tabs>
+     <Tabs.Panel value="tab2">
+       <p className="py-4 text-sm text-zinc-600 dark:text-zinc-400">Security settings panel content.</p>
+     </Tabs.Panel>
+     <Tabs.Panel value="tab3">
+       <p className="py-4 text-sm text-zinc-600 dark:text-zinc-400">Notification preferences panel content.</p>
+     </Tabs.Panel>
+   </Tabs.Panels>
+ </Tabs>
```

### resources/js/react-demos/pages/TextDemo.tsx — Colors

```diff
--- code prop
+++ rendered JSX
- <Text color="default">Default text</Text>
+ <Text color="default">Default text color</Text>
- <Text color="muted">Muted text</Text>
+ <Text color="muted">Muted text for secondary information</Text>
- <Text color="accent">Accent text</Text>
+ <Text color="accent">Accent text for emphasis</Text>
- <Text color="success">Success text</Text>
+ <Text color="success">Success text for positive feedback</Text>
- <Text color="danger">Danger text</Text>
+ <Text color="danger">Danger text for errors and warnings</Text>
```

### resources/js/react-demos/pages/TextDemo.tsx — Sizes

```diff
--- code prop
+++ rendered JSX
- <Text size="xs">Extra small</Text>
+ <Text size="xs">Extra small text</Text>
- <Text size="sm">Small</Text>
+ <Text size="sm">Small text</Text>
- <Text size="md">Medium</Text>
+ <Text size="md">Medium text (default)</Text>
- <Text size="lg">Large</Text>
+ <Text size="lg">Large text</Text>
```

### resources/js/react-demos/pages/TextDemo.tsx — Weights

```diff
--- code prop
+++ rendered JSX
- <Text weight="normal">Normal</Text>
+ <Text weight="normal">Normal weight</Text>
- <Text weight="medium">Medium</Text>
+ <Text weight="medium">Medium weight</Text>
- <Text weight="semibold">Semibold</Text>
+ <Text weight="semibold">Semibold weight</Text>
- <Text weight="bold">Bold</Text>
+ <Text weight="bold">Bold weight</Text>
```

### resources/js/react-demos/pages/TimelineDemo.tsx — Horizontal (data-driven)

```diff
--- code prop
+++ rendered JSX
- <Timeline events={events} variant="horizontal" />
+ <Timeline events={companyTimeline} variant="horizontal" />
```

### resources/js/react-demos/pages/TimelineDemo.tsx — Stacked (data-driven)

```diff
--- code prop
+++ rendered JSX
- <Timeline events={events} />
+ <Timeline events={companyTimeline} />
```

### resources/js/react-demos/pages/TimelineDemo.tsx — Alternating (data-driven)

```diff
--- code prop
+++ rendered JSX
- <Timeline events={events} variant="alternating" />
+ <Timeline events={companyTimeline} variant="alternating" />
```

### resources/js/react-demos/pages/TimelineDemo.tsx — Compound Components

```diff
--- code prop
+++ rendered JSX
  <Timeline>
-   <Timeline.Item color="green" icon={<CheckIcon />} date="Feb 1">
+   <Timeline.Item icon={<CheckIcon />} color="green" date="Feb 1">
      <p className="font-medium">Requirements gathered</p>
-   </Timeline.Item>
+     <p className="text-sm text-zinc-500">Completed on schedule</p>
-   <Timeline.Item color="blue" active date="March 1">
+   </Timeline.Item>
-     <p className="font-medium">Development in progress</p>
+   <Timeline.Item icon={<CheckIcon />} color="green" date="Feb 15">
-   </Timeline.Item>
+     <p className="font-medium">Design approved</p>
-   <Timeline.Item color="zinc">
+     <p className="text-sm text-zinc-500">Stakeholders signed off</p>
-     <p className="font-medium text-zinc-400">Testing</p>
+   </Timeline.Item>
-   </Timeline.Item>
+   <Timeline.Item icon={<RocketIcon />} color="blue" active date="March 1">
- </Timeline>
+     <p className="font-medium">Development in progress</p>
+     <p className="text-sm text-zinc-500">Sprint 3 of 5</p>
+   </Timeline.Item>
+   <Timeline.Item color="zinc">
+     <p className="font-medium text-zinc-400">Testing</p>
+     <p className="text-sm text-zinc-400">Not started</p>
+   </Timeline.Item>
+ </Timeline>
```

### resources/js/react-demos/pages/TimelineDemo.tsx — Blocks

```diff
--- code prop
+++ rendered JSX
  <Timeline>
-   <Timeline.Block heading="v2.0 Released" icon={<CheckIcon />} color="green">
+   <Timeline.Block heading="v2.0.0 Released" icon={<CheckIcon />} color="green">
-     Major release with new features.
+     Major release with new dashboard, API v2, and performance improvements.
    </Timeline.Block>
- </Timeline>
+   <Timeline.Block heading="Security Patch" icon={<CheckIcon />} color="green">
+     CVE-2026-1234 patched in authentication module.
+   </Timeline.Block>
+   <Timeline.Block heading="Feature Merged" color="blue" active>
+     <p>Analytics module merged into main.</p>
+     <ul className="mt-2 list-inside list-disc text-zinc-500 dark:text-zinc-400">
+       <li>Real-time event tracking</li>
+       <li>Custom dashboard widgets</li>
+       <li>Export to CSV/PDF</li>
+     </ul>
+   </Timeline.Block>
+   <Timeline.Block heading="Upcoming: Mobile Launch" color="zinc">
+     iOS and Android apps scheduled for next quarter.
+   </Timeline.Block>
+ </Timeline>
```

### resources/js/react-demos/pages/TimePickerDemo.tsx — 12-Hour Format

```diff
--- code prop
+++ rendered JSX
- <TimePicker value={time} onChange={setTime} format="12h" />
+ <TimePicker value={time12} onChange={setTime12} format="12h" />
+ <p className="mt-3 text-sm text-zinc-500">Value: {time12}</p>
```

### resources/js/react-demos/pages/TimePickerDemo.tsx — 24-Hour Format

```diff
--- code prop
+++ rendered JSX
- <TimePicker value={time} onChange={setTime} format="24h" />
+ <TimePicker value={time24} onChange={setTime24} format="24h" />
+ <p className="mt-3 text-sm text-zinc-500">Value: {time24}</p>
```

### resources/js/react-demos/pages/ToastDemo.tsx — Variants

```diff
--- code prop
+++ rendered JSX
- const { toast } = useToast();
+ <ToastButtons />
- 
- toast({ title: "Saved!", variant: "success" });
- toast({ title: "Error", description: "Something went wrong.", variant: "error" });
- toast({ title: "Warning", variant: "warning" });
- toast({ title: "Info", variant: "info" });
```

### resources/js/react-demos/pages/ToastDemo.tsx — Usage

```diff
--- code prop
+++ rendered JSX
- <Toast.Provider position="bottom-right">
+ <pre className="rounded-lg bg-zinc-100 p-4 text-sm dark:bg-zinc-800">
-   <App />
+ {`<Toast.Provider position="bottom-right">
- </Toast.Provider>
+   <App />
- 
+ </Toast.Provider>
- // In a component:
+ 
- const { toast } = useToast();
+ // In a component:
- toast({ title: "Hello!", variant: "success" });
+ const { toast } = useToast();
+ toast({ title: "Hello!", variant: "success" });`}
+           </pre>
```

### resources/js/react-demos/pages/TooltipDemo.tsx — Placements

```diff
--- code prop
+++ rendered JSX
  <Tooltip content="Appears on top" placement="top">
    <Action>Top</Action>
  </Tooltip>
  <Tooltip content="Appears on bottom" placement="bottom">
    <Action>Bottom</Action>
  </Tooltip>
+ <Tooltip content="Appears on left" placement="left">
+   <Action>Left</Action>
+ </Tooltip>
+ <Tooltip content="Appears on right" placement="right">
+   <Action>Right</Action>
+ </Tooltip>
```

### resources/js/react-demos/pages/TooltipDemo.tsx — Rich Content

```diff
--- code prop
+++ rendered JSX
- <Tooltip content={
+ <Tooltip
-   <div>
+   content={
-     <p className="font-semibold">Keyboard shortcut</p>
+     <div>
-     <p>Ctrl + S</p>
+       <p className="font-semibold">Keyboard shortcut</p>
-   </div>
+       <p className="text-zinc-300">Ctrl + S</p>
- }>
+     </div>
-   <Action>Hover for details</Action>
+   }
- </Tooltip>
+ >
+   <Action>Hover for details</Action>
+ </Tooltip>
```

## Dynamic (skipped — needs human audit)

| File | Section | Note |
|---|---|---|
| resources/js/react-demos/pages/AccordionDemo.tsx | Single | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/AccordionDemo.tsx | Multiple | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/AutocompleteDemo.tsx | Basic | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/AutocompleteDemo.tsx | With Disabled Options | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/BadgeDemo.tsx | Colors | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CalendarDemo.tsx | Single Date | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CalendarDemo.tsx | Date Range | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CalloutDemo.tsx | Dismissible | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CarouselDemo.tsx | Basic Carousel | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CarouselDemo.tsx | AutoPlay | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CarouselDemo.tsx | Loop | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CarouselDemo.tsx | AutoPlay + Loop | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/ChartDemo.tsx | Sparkline | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CodeEditorDemo.tsx | Language Switching | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/CodeEditorDemo.tsx | Theme Showcase | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/ColorPickerDemo.tsx | With Presets | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/ComposerDemo.tsx | Basic | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/DiagramDemo.tsx | ERD Diagram | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/DiagramDemo.tsx | Declarative API | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/DynamicCarouselDemo.tsx | Dynamic Slides | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/EditorDemo.tsx | Basic Editor | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/EditorDemo.tsx | Line Spacing | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/EditorDemo.tsx | Markdown Output | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/EditorDemo.tsx | Editor with Extensions | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/EditorDemo.tsx | AI Chat Stream | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/EmojiSelectDemo.tsx | Searchable Picker | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/IconDemo.tsx | Named Icons (Lucide) | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/IconDemo.tsx | Sizes | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/IdeDemo.tsx | Full IDE Layout | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/InputsDemo.tsx | Select | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/InputsDemo.tsx | CheckboxGroup | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/InputsDemo.tsx | RadioGroup | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/InputsDemo.tsx | Slider | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/KanbanDemo.tsx | Drag & Drop Board | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MenuDemo.tsx | Horizontal Menu | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MenuDemo.tsx | Vertical Menu | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MobileMenuDemo.tsx | Flyout Menu (Left) | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MobileMenuDemo.tsx | Bottom Bar | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MultiSwitchDemo.tsx | Basic | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MultiSwitchDemo.tsx | With Labels & Object Options | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MultiSwitchDemo.tsx | Sizes | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MultiSwitchDemo.tsx | States | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/MultiSwitchDemo.tsx | Linear Mode | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/OtpInputDemo.tsx | 6-Digit Code | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/OtpInputDemo.tsx | 4-Digit Code | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/PillboxDemo.tsx | Basic | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/PillboxDemo.tsx | Disabled | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/PopoverDemo.tsx | Placements | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/SelectDemo.tsx | Multi-Select | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/SelectDemo.tsx | Searchable Multi-Select | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/SidebarDemo.tsx | Controlled Collapsed State | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/TableDemo.tsx | Compound Table | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/TreeNavDemo.tsx | File Tree | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/TreeNavDemo.tsx | Expand All | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/TreeNavDemo.tsx | No Icons | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |
| resources/js/react-demos/pages/WizardDemo.tsx | Multi-Step Wizard | children contain .map()/ternary/dynamic JSX — canonical `code` must be hand-verified |

## Missing code prop

| File | Section | Note |
|---|---|---|
| resources/js/react-demos/pages/CheckboxDemo.tsx | Single Checkbox | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/CheckboxDemo.tsx | Indeterminate | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/CheckboxDemo.tsx | States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/CheckboxDemo.tsx | Checkbox Group | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/CheckboxDemo.tsx | Horizontal Group | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/DatePickerDemo.tsx | Basic | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/DatePickerDemo.tsx | With Time | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/DatePickerDemo.tsx | Date Range | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/DatePickerDemo.tsx | With Constraints | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/DatePickerDemo.tsx | States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/InputDemo.tsx | Basic | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/InputDemo.tsx | With Leading & Trailing | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/InputDemo.tsx | Sizes | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/InputDemo.tsx | States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/InputDemo.tsx | Controlled | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/PopoverDemo.tsx | Rich Content (Hover) | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/RadioGroupDemo.tsx | Basic | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/RadioGroupDemo.tsx | Horizontal | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/RadioGroupDemo.tsx | States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SelectDemo.tsx | Basic (Native) | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SelectDemo.tsx | Option Groups | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SelectDemo.tsx | States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SelectDemo.tsx | Controlled | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SelectDemo.tsx | Listbox States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SliderDemo.tsx | Basic | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SliderDemo.tsx | With Marks | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SliderDemo.tsx | Range Slider | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SliderDemo.tsx | States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SwitchDemo.tsx | Basic | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SwitchDemo.tsx | Colors | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SwitchDemo.tsx | Sizes | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/SwitchDemo.tsx | States | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/TextareaDemo.tsx | Basic | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/TextareaDemo.tsx | Auto Resize | no `code` prop — intentional preview-only? |
| resources/js/react-demos/pages/TextareaDemo.tsx | States | no `code` prop — intentional preview-only? |

## Matches (by file)

- `resources/js/react-demos/pages/ActionDemo.tsx` — 15 section(s): "State Variants", "Alert Animation", "Size Variants", "Standalone Colors", "With Icons", "Alert Icons", "Emoji Support", "Avatar & Badge", "Circle Variant", "Ghost Variant", "Sort Control", "Disabled State", "Loading State", "As Link", "Toolbar Example"
- `resources/js/react-demos/pages/AvatarDemo.tsx` — 2 section(s): "Sizes", "Status"
- `resources/js/react-demos/pages/BadgeDemo.tsx` — 3 section(s): "Variants", "Sizes", "With Dot"
- `resources/js/react-demos/pages/BrandDemo.tsx` — 3 section(s): "Basic", "Sizes", "Name Only"
- `resources/js/react-demos/pages/BreadcrumbsDemo.tsx` — 3 section(s): "Basic", "Deep Nesting", "Shrink"
- `resources/js/react-demos/pages/CalloutDemo.tsx` — 1 section(s): "Colors"
- `resources/js/react-demos/pages/ChartDemo.tsx` — 4 section(s): "Bar Chart", "Donut Chart", "Pie Chart", "Horizontal Bar Chart"
- `resources/js/react-demos/pages/ColorPickerDemo.tsx` — 2 section(s): "Sizes", "Disabled"
- `resources/js/react-demos/pages/ComposerDemo.tsx` — 1 section(s): "Disabled"
- `resources/js/react-demos/pages/DropdownDemo.tsx` — 2 section(s): "Basic", "With Disabled Items"
- `resources/js/react-demos/pages/FileUploadDemo.tsx` — 2 section(s): "Thumbnail List", "Disabled"
- `resources/js/react-demos/pages/IconDemo.tsx` — 4 section(s): "Colors", "Inside Action Buttons", "Inside Callouts", "Inside Badges"
- `resources/js/react-demos/pages/InputsDemo.tsx` — 1 section(s): "Input — Sizes"
- `resources/js/react-demos/pages/OtpInputDemo.tsx` — 1 section(s): "Disabled"
- `resources/js/react-demos/pages/ProfileDemo.tsx` — 1 section(s): "Sizes"
- `resources/js/react-demos/pages/ProgressDemo.tsx` — 1 section(s): "Sizes"
- `resources/js/react-demos/pages/SkeletonDemo.tsx` — 1 section(s): "Shapes"
- `resources/js/react-demos/pages/TimePickerDemo.tsx` — 2 section(s): "Minute Steps", "Disabled"
- `resources/js/react-demos/pages/TimelineDemo.tsx` — 3 section(s): "Horizontal Minimal", "With Heading", "No Animation"
